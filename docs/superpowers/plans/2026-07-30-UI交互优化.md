# UI 与交互优化 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在既有功能与后端契约完全不变的前提下，重塑 `web/` 前端的视觉（手绘草图风 + 白纸/黑板双主题）与关键交互（生成状态反馈、消除所有 `alert`、分部规划升级为可采纳面板）。

**Architecture:** 纯前端改造。新增 3 个可单测的纯函数模块（主题工具、toast reducer、分部标题解析），新增 4 个组件（TopBar / Toast / SectionPlanPanel），全量重写 `styles.css`（CSS 变量双主题 + 手绘基元 + 三区布局 + 动画），重构 5 个既有组件，最后在 `App.tsx` 接线（生成态锁定、阶段文案、alert→toast、完成反馈）。不引入任何新的运行时依赖或 CSS 框架。

**Tech Stack:** React 18 + Vite + TypeScript（strict, noUnusedLocals, noUnusedParameters）；vitest（node 环境，无 jsdom）；纯 CSS + CSS 变量。

## Global Constraints

- **不改后端**：不改任何 `server/` 路由、不改数据结构、不改 `web/src/api.ts` 与 `web/src/store.ts` 的既有数据契约（可新增前端 UI 辅助纯函数，但不得改动既有导出签名）。
- **不引依赖**：不新增任何 npm 包（无 Tailwind/MUI/rough.js/sonner/jsdom）。所有单测必须在现有 vitest **node 默认环境**下通过——因此凡涉及 `localStorage`/`document`/React 渲染的逻辑，必须把可测部分抽成不触达浏览器全局的纯函数来测。
- **类型零错误**：`tsc -b` 必须零错误，注意 `noUnusedLocals` 与 `noUnusedParameters`（未用的 import/参数会直接编译失败）。
- **构建通过**：`npm run build`（即 `tsc -b && vite build`）必须成功产出 `web/dist`。
- **现有测试全绿**：`web/src/store.test.ts`、`web/src/api.test.ts` 不得回归。
- **主题机制**：根元素 `document.documentElement` 挂 `data-theme="paper" | "blackboard"`；localStorage 键固定为 `novelbox-theme`；默认 `paper`。
- **所有颜色走 CSS 变量**（见 Task 1 变量表），组件内不得写死颜色。
- **命名**：语义化、简单直接（与用户既有偏好一致）。中文注释风格与既有代码一致（简短、贴代码）。
- 所有 `web` 命令在 `web/` 目录下执行（`cwd=web`）。

---

## File Structure

新增文件：
- `web/src/theme.ts` — 主题读写与纯逻辑（`normalizeTheme`/`nextTheme` 纯函数 + `getTheme`/`setTheme`/`toggleTheme`/`applyStoredTheme` 副作用函数）
- `web/src/theme.test.ts` — 主题纯函数单测
- `web/src/components/Toast.tsx` — `ToastProvider` + `useToast` + `toastReducer`（纯）
- `web/src/toast.test.ts` — `toastReducer` 单测
- `web/src/sections.ts` — `parseSectionTitles`（纯）分部标题解析
- `web/src/sections.test.ts` — 解析单测
- `web/src/components/SectionPlanPanel.tsx` — 分部规划面板（流式 + 采纳建部）
- `web/src/components/TopBar.tsx` — 顶栏（书名 + 主题切换 + 设置入口 + 生成阶段文案）
- `web/public/fonts/` — 手写体字体（best-effort 打包，系统字体兜底）

重写/重构文件：
- `web/src/styles.css` — 全量重写（各 Task 追加自己的分区）
- `web/src/main.tsx` — 启动应用主题 + 包裹 `ToastProvider`
- `web/src/components/Sidebar.tsx` — 手绘树、禁用态、骨架、空状态、按钮迁移
- `web/src/components/MainPanel.tsx` — 稿纸卡片、流式光标、回退按钮位、空状态
- `web/src/components/Actions.tsx` — 手绘按钮、停止态、抽打框
- `web/src/components/SettingsPage.tsx` — 手绘表单
- `web/src/components/FirstRun.tsx` — 手绘开篇页
- `web/src/App.tsx` — 接线全部：TopBar/Toast/面板、生成态锁定、阶段文案、alert→toast、完成反馈、加载骨架

---

### Task 1: 主题系统与设计基元

建立整套视觉地基：主题纯逻辑 + 副作用封装、CSS 变量双主题、手绘描边基元、字体、全局 reset。这是后续所有任务的依赖。

**Files:**
- Create: `web/src/theme.ts`
- Create: `web/src/theme.test.ts`
- Modify: `web/src/main.tsx`
- Rewrite: `web/src/styles.css`
- Create dir: `web/public/fonts/`

**Interfaces:**
- Produces:
  - `web/src/theme.ts`：
    - `export type Theme = 'paper' | 'blackboard'`
    - `export function normalizeTheme(raw: string | null): Theme`（纯）
    - `export function nextTheme(cur: Theme): Theme`（纯）
    - `export function getTheme(): Theme`
    - `export function setTheme(t: Theme): void`
    - `export function toggleTheme(): Theme`
    - `export function applyStoredTheme(): void`
  - `web/src/styles.css`：定义 CSS 变量、`.sketch` 基元、`@font-face`、全局 reset、闪烁光标 `@keyframes blink`（供后续任务使用）。

- [ ] **Step 1: 写失败测试 `web/src/theme.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { normalizeTheme, nextTheme } from './theme';

describe('theme 纯逻辑', () => {
  it('normalizeTheme 识别 blackboard', () => {
    expect(normalizeTheme('blackboard')).toBe('blackboard');
  });
  it('normalizeTheme 其它值一律回退 paper', () => {
    expect(normalizeTheme('paper')).toBe('paper');
    expect(normalizeTheme(null)).toBe('paper');
    expect(normalizeTheme('乱七八糟')).toBe('paper');
  });
  it('nextTheme 在两主题间切换', () => {
    expect(nextTheme('paper')).toBe('blackboard');
    expect(nextTheme('blackboard')).toBe('paper');
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test` （cwd=`web`）
Expected: FAIL —— `Cannot find module './theme'`。

- [ ] **Step 3: 写 `web/src/theme.ts`**

```ts
export type Theme = 'paper' | 'blackboard';

const KEY = 'novelbox-theme';

// 纯逻辑：任意输入归一到合法主题，非 blackboard 一律 paper
export function normalizeTheme(raw: string | null): Theme {
  return raw === 'blackboard' ? 'blackboard' : 'paper';
}

// 纯逻辑：两主题互切
export function nextTheme(cur: Theme): Theme {
  return cur === 'paper' ? 'blackboard' : 'paper';
}

export function getTheme(): Theme {
  return normalizeTheme(localStorage.getItem(KEY));
}

export function setTheme(t: Theme): void {
  localStorage.setItem(KEY, t);
  document.documentElement.setAttribute('data-theme', t);
}

export function toggleTheme(): Theme {
  const t = nextTheme(getTheme());
  setTheme(t);
  return t;
}

// 启动时把已存主题写到 <html data-theme>
export function applyStoredTheme(): void {
  document.documentElement.setAttribute('data-theme', getTheme());
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS（theme 3 例 + 既有 store/api 全绿）。

- [ ] **Step 5: 全量重写 `web/src/styles.css`（地基层）**

替换整个文件为以下内容（后续任务在文件末尾各自追加分区）：

```css
/* ===== 字体（best-effort 打包，系统手写体兜底）===== */
@font-face {
  font-family: 'NovelHand';
  src: url('/fonts/handwriting.woff2') format('woff2');
  font-display: swap;
}

/* ===== 主题变量 ===== */
:root[data-theme="paper"] {
  --bg: #f4efe4; --panel: #fbf8f0;
  --ink: #2b2620; --ink-soft: #6b6154;
  --line: #3a332a; --accent: #c0392b; --accent-2: #2e7d68;
  --shadow: rgba(0,0,0,.15);
}
:root[data-theme="blackboard"] {
  --bg: #26302c; --panel: #2f3a35;
  --ink: #f0ebe0; --ink-soft: #b8b0a0;
  --line: #d8d0c0; --accent: #e8a24a; --accent-2: #7fc8a9;
  --shadow: rgba(0,0,0,.4);
}
/* 未设属性时的默认（= paper） */
:root { color-scheme: light; }

/* ===== 全局 reset & 排版 ===== */
* { box-sizing: border-box; }
html, body, #root { height: 100%; margin: 0; }
body {
  background: var(--bg);
  color: var(--ink);
  font-family: 'NovelHand', 'KaiTi', 'STKaiti', '楷体', 'Comic Sans MS', cursive, sans-serif;
  font-size: 16px;
  line-height: 1.7;
  transition: background .3s, color .3s;
}
h1, h2, h3 { font-weight: 700; letter-spacing: .5px; }
button { font-family: inherit; cursor: pointer; }
textarea, input { font-family: inherit; color: var(--ink); }
pre { white-space: pre-wrap; word-break: break-word; font-family: inherit; margin: 0; }

/* ===== 手绘描边基元 ===== */
.sketch {
  border: 2px solid var(--line);
  border-radius: 255px 15px 225px 15px / 15px 225px 15px 255px;
  box-shadow: 2px 2px 0 var(--shadow);
  background: var(--panel);
}
.sketch-alt {
  border-radius: 15px 225px 15px 255px / 225px 15px 255px 15px;
}

/* ===== 通用手绘按钮 ===== */
.hbtn {
  border: 2px solid var(--line);
  border-radius: 15px 225px 15px 255px / 225px 15px 255px 15px;
  background: var(--panel);
  color: var(--ink);
  padding: 8px 16px;
  box-shadow: 2px 2px 0 var(--shadow);
  transition: transform .08s, box-shadow .08s;
}
.hbtn:hover { transform: translate(-1px,-1px); box-shadow: 3px 3px 0 var(--shadow); }
.hbtn:active { transform: translate(1px,1px); box-shadow: 1px 1px 0 var(--shadow); }
.hbtn:disabled { opacity: .45; cursor: not-allowed; box-shadow: none; }
.hbtn.dashed { border-style: dashed; box-shadow: none; }
.hbtn.accent { border-color: var(--accent); color: var(--accent); }
.hbtn.accent-2 { border-color: var(--accent-2); color: var(--accent-2); }

/* ===== 流式光标 ===== */
@keyframes blink { 0%,49% { opacity: 1 } 50%,100% { opacity: 0 } }
.cursor { color: var(--accent); animation: blink 1s step-end infinite; font-weight: 700; }
```

- [ ] **Step 6: 打包手写字体（best-effort，不阻塞）**

创建目录并尝试下载一款开源可商用手写体（霞鹜文楷 Lite）到 `web/public/fonts/handwriting.woff2`：

Run（cwd=`web`）:
```bash
mkdir -p public/fonts
curl -fsSL -o public/fonts/handwriting.woff2 \
  "https://cdn.jsdelivr.net/npm/lxgw-wenkai-lite-webfont@1.7.0/style/fonts/LXGWWenKaiLite-Regular.woff2" \
  || echo "字体下载失败：跳过，@font-face 会自动回退到系统楷体（KaiTi/STKaiti/楷体）"
```
说明：若企业网络拦截下载失败，无需处理——`@font-face` 的 `font-display: swap` 会在字体缺失时立即回退到 CSS 字体栈里的系统楷体，视觉仍成立、构建仍通过。不要因下载失败而阻塞。

- [ ] **Step 7: 改 `web/src/main.tsx` 启动时应用主题**

```tsx
import './styles.css';
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { applyStoredTheme } from './theme';

applyStoredTheme();

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

- [ ] **Step 8: 类型检查 + 构建**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` 零错误，vite 产出 `dist` 成功。

- [ ] **Step 9: 提交**

```bash
git add web/src/theme.ts web/src/theme.test.ts web/src/styles.css web/src/main.tsx web/public/fonts
git commit -m "feat(ui): 主题系统与手绘设计基元（双主题变量+.sketch+字体+光标）"
```

---

### Task 2: Toast 系统（消除 alert 的基础设施）

轻量自研 toast：可单测的纯 reducer + `ToastProvider` + `useToast`，右上角手绘卡片堆叠、自动消失。App 在 Task 7 才真正调用它替换 alert；本任务只交付可用的 Provider/hook 与样式，并用 `ToastProvider` 包裹 App。

**Files:**
- Create: `web/src/components/Toast.tsx`
- Create: `web/src/toast.test.ts`
- Modify: `web/src/main.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: 无（独立）。
- Produces（`web/src/components/Toast.tsx`）：
  - `export type ToastType = 'success' | 'error' | 'info'`
  - `export interface ToastItem { id: number; type: ToastType; msg: string }`
  - `export type ToastAction = { kind: 'add'; toast: ToastItem } | { kind: 'remove'; id: number }`
  - `export function toastReducer(state: ToastItem[], action: ToastAction): ToastItem[]`（纯）
  - `export interface ToastApi { success(msg: string): void; error(msg: string): void; info(msg: string): void }`
  - `export function useToast(): ToastApi`
  - `export function ToastProvider({ children }: { children: React.ReactNode }): JSX.Element`

- [ ] **Step 1: 写失败测试 `web/src/toast.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { toastReducer } from './components/Toast';
import type { ToastItem } from './components/Toast';

const a: ToastItem = { id: 1, type: 'success', msg: 'A' };
const b: ToastItem = { id: 2, type: 'error', msg: 'B' };

describe('toastReducer', () => {
  it('add 追加到末尾', () => {
    expect(toastReducer([a], { kind: 'add', toast: b })).toEqual([a, b]);
  });
  it('remove 按 id 删除', () => {
    expect(toastReducer([a, b], { kind: 'remove', id: 1 })).toEqual([b]);
  });
  it('remove 不存在的 id 原样返回', () => {
    expect(toastReducer([a], { kind: 'remove', id: 99 })).toEqual([a]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL —— 找不到模块 `./components/Toast`。

- [ ] **Step 3: 写 `web/src/components/Toast.tsx`**

```tsx
import { createContext, useCallback, useContext, useReducer, useRef } from 'react';
import type { ReactNode } from 'react';

export type ToastType = 'success' | 'error' | 'info';
export interface ToastItem { id: number; type: ToastType; msg: string; }
export type ToastAction =
  | { kind: 'add'; toast: ToastItem }
  | { kind: 'remove'; id: number };

// 纯 reducer
export function toastReducer(state: ToastItem[], action: ToastAction): ToastItem[] {
  if (action.kind === 'add') return [...state, action.toast];
  return state.filter((t) => t.id !== action.id);
}

export interface ToastApi {
  success(msg: string): void;
  error(msg: string): void;
  info(msg: string): void;
}

const Ctx = createContext<ToastApi | null>(null);

export function useToast(): ToastApi {
  const api = useContext(Ctx);
  if (!api) throw new Error('useToast 必须在 ToastProvider 内使用');
  return api;
}

export function ToastProvider({ children }: { children: ReactNode }) {
  const [items, dispatch] = useReducer(toastReducer, []);
  const seq = useRef(0);

  const push = useCallback((type: ToastType, msg: string) => {
    const id = ++seq.current;
    dispatch({ kind: 'add', toast: { id, type, msg } });
    // error 停留更久，需要用户注意
    const ttl = type === 'error' ? 6000 : 3500;
    setTimeout(() => dispatch({ kind: 'remove', id }), ttl);
  }, []);

  const api = useRef<ToastApi>({
    success: (m) => push('success', m),
    error: (m) => push('error', m),
    info: (m) => push('info', m),
  }).current;

  return (
    <Ctx.Provider value={api}>
      {children}
      <div className="toast-stack">
        {items.map((t) => (
          <div key={t.id} className={`toast sketch ${t.type}`}>
            <span>{t.msg}</span>
            <button className="toast-x" onClick={() => dispatch({ kind: 'remove', id: t.id })}>×</button>
          </div>
        ))}
      </div>
    </Ctx.Provider>
  );
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS（toast 3 例 + 既有全绿）。

- [ ] **Step 5: 追加 toast 样式到 `web/src/styles.css`**

```css
/* ===== Toast ===== */
.toast-stack {
  position: fixed; top: 16px; right: 16px; z-index: 1000;
  display: flex; flex-direction: column; gap: 10px; max-width: 320px;
}
.toast {
  display: flex; align-items: center; gap: 10px;
  padding: 10px 12px; color: var(--ink);
  animation: toast-in .18s ease-out;
}
@keyframes toast-in { from { transform: translateX(20px); opacity: 0 } to { transform: none; opacity: 1 } }
.toast.success { border-color: var(--accent-2); }
.toast.error { border-color: var(--accent); }
.toast .toast-x {
  margin-left: auto; border: none; background: transparent;
  color: var(--ink-soft); font-size: 18px; line-height: 1;
}
```

- [ ] **Step 6: 用 `ToastProvider` 包裹 App（`web/src/main.tsx`）**

```tsx
import './styles.css';
import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './App';
import { applyStoredTheme } from './theme';
import { ToastProvider } from './components/Toast';

applyStoredTheme();

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ToastProvider>
      <App />
    </ToastProvider>
  </React.StrictMode>,
);
```

- [ ] **Step 7: 类型检查 + 构建**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` 零错误，构建成功。

- [ ] **Step 8: 提交**

```bash
git add web/src/components/Toast.tsx web/src/toast.test.ts web/src/main.tsx web/src/styles.css
git commit -m "feat(ui): 自研 Toast 系统（纯 reducer + Provider/useToast + 手绘卡片）"
```

---

### Task 3: 分部标题解析 + 分部规划面板

`parseSectionTitles` 纯函数（把 AI 分部建议文本解析成标题数组），以及 `SectionPlanPanel` 组件（流式显示建议 + 采纳批量建部）。App 在 Task 7 接线；本任务交付纯函数、组件与样式。

**Files:**
- Create: `web/src/sections.ts`
- Create: `web/src/sections.test.ts`
- Create: `web/src/components/SectionPlanPanel.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `useToast`（Task 2，可选，本任务组件不直接用，采纳后的 toast 在 App 里发）。
- Produces:
  - `web/src/sections.ts`：`export function parseSectionTitles(text: string): string[]`（纯）
  - `web/src/components/SectionPlanPanel.tsx`：
    - `export function SectionPlanPanel(props): JSX.Element`，props：
      - `text: string`（流式建议全文）
      - `streaming: boolean`
      - `onAdopt: (titles: string[]) => void`
      - `onClose: () => void`

- [ ] **Step 1: 写失败测试 `web/src/sections.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { parseSectionTitles } from './sections';

describe('parseSectionTitles', () => {
  it('解析「第 N 部 · 标题」', () => {
    const t = '第 1 部 · 迷雾初现\n第 2 部 · 真相追踪';
    expect(parseSectionTitles(t)).toEqual(['迷雾初现', '真相追踪']);
  });
  it('兼容「第N部：标题」「第N部 标题」多种分隔', () => {
    const t = '第1部：开端\n第2部 发展\n第3部-高潮';
    expect(parseSectionTitles(t)).toEqual(['开端', '发展', '高潮']);
  });
  it('忽略无法匹配的行与空行', () => {
    const t = '下面是我的规划：\n\n第1部 序章\n随便一句话\n';
    expect(parseSectionTitles(t)).toEqual(['序章']);
  });
  it('标题缺失时用「第 N 部」兜底', () => {
    expect(parseSectionTitles('第1部\n第2部')).toEqual(['第 1 部', '第 2 部']);
  });
  it('全无匹配返回空数组', () => {
    expect(parseSectionTitles('毫无结构的一段话')).toEqual([]);
  });
});
```

- [ ] **Step 2: 运行测试确认失败**

Run: `npm test`
Expected: FAIL —— 找不到模块 `./sections`。

- [ ] **Step 3: 写 `web/src/sections.ts`**

```ts
// 从 AI 分部建议文本里逐行解析出分部标题。
// 匹配「第 N 部」开头的行，标题为其后经分隔符（·:：\-—、空格）连接的内容；
// 无标题时用「第 N 部」兜底。
export function parseSectionTitles(text: string): string[] {
  const out: string[] = [];
  for (const raw of text.split('\n')) {
    const line = raw.trim();
    const m = line.match(/^第\s*(\d+)\s*部\s*[·:：\-—、.]?\s*(.*)$/);
    if (!m) continue;
    const title = m[2].trim();
    out.push(title || `第 ${m[1]} 部`);
  }
  return out;
}
```

- [ ] **Step 4: 运行测试确认通过**

Run: `npm test`
Expected: PASS（sections 5 例 + 既有全绿）。

- [ ] **Step 5: 写 `web/src/components/SectionPlanPanel.tsx`**

```tsx
import { parseSectionTitles } from '../sections';

export function SectionPlanPanel({ text, streaming, onAdopt, onClose }: {
  text: string;
  streaming: boolean;
  onAdopt: (titles: string[]) => void;
  onClose: () => void;
}) {
  const titles = parseSectionTitles(text);
  return (
    <div className="modal-mask" onClick={onClose}>
      <div className="plan-panel sketch" onClick={(e) => e.stopPropagation()}>
        <h2>🧩 AI 分部规划</h2>
        <pre className="plan-text">
          {text || '正在思考…'}
          {streaming && <span className="cursor">▎</span>}
        </pre>
        {!streaming && titles.length > 0 && (
          <div className="plan-preview">
            将创建 {titles.length} 个部：
            <ol>{titles.map((t, i) => <li key={i}>{t}</li>)}</ol>
          </div>
        )}
        <div className="plan-actions">
          <button className="hbtn accent-2" disabled={streaming || titles.length === 0}
            onClick={() => onAdopt(titles)}>✓ 采纳并创建这些部</button>
          <button className="hbtn" onClick={onClose}>取消</button>
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 6: 追加面板样式到 `web/src/styles.css`**

```css
/* ===== 模态遮罩 & 分部面板 ===== */
.modal-mask {
  position: fixed; inset: 0; z-index: 900;
  background: rgba(0,0,0,.35);
  display: flex; align-items: center; justify-content: center;
  padding: 24px;
}
.plan-panel {
  width: min(560px, 92vw); max-height: 82vh; overflow: auto;
  padding: 20px 24px; display: flex; flex-direction: column; gap: 14px;
}
.plan-panel h2 { margin: 0; }
.plan-text {
  min-height: 120px; max-height: 40vh; overflow: auto;
  padding: 12px; border: 1px dashed var(--line); border-radius: 8px;
  color: var(--ink); line-height: 1.8;
}
.plan-preview { color: var(--ink-soft); }
.plan-preview ol { margin: 6px 0 0; padding-left: 22px; color: var(--ink); }
.plan-actions { display: flex; gap: 12px; justify-content: flex-end; }
```

- [ ] **Step 7: 类型检查 + 构建**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` 零错误，构建成功。

- [ ] **Step 8: 提交**

```bash
git add web/src/sections.ts web/src/sections.test.ts web/src/components/SectionPlanPanel.tsx web/src/styles.css
git commit -m "feat(ui): 分部标题解析纯函数 + 可采纳分部规划面板"
```

---

### Task 4: 顶栏 TopBar（书名 + 主题切换 + 设置入口 + 阶段文案）

新增顶栏组件。主题切换按钮内部自管理主题状态（调用 `toggleTheme` 并更新标签）；生成阶段文案由 App 通过 props 传入。设置入口回调交给 App。

**Files:**
- Create: `web/src/components/TopBar.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `getTheme`/`toggleTheme`/`Theme`（Task 1）。
- Produces（`web/src/components/TopBar.tsx`）：
  - `export function TopBar(props): JSX.Element`，props：
    - `title: string`
    - `streaming: boolean`
    - `statusText: string`（生成阶段文案，如 `✍️ 正在写第 3 章…`，非生成态为空串）
    - `onOpenSettings: () => void`

- [ ] **Step 1: 写 `web/src/components/TopBar.tsx`**

（此组件为纯视觉，无独立单测；正确性由 `tsc` + 手动验收保证。）

```tsx
import { useState } from 'react';
import { getTheme, toggleTheme } from '../theme';
import type { Theme } from '../theme';

export function TopBar({ title, streaming, statusText, onOpenSettings }: {
  title: string;
  streaming: boolean;
  statusText: string;
  onOpenSettings: () => void;
}) {
  const [theme, setTheme] = useState<Theme>(getTheme());
  const label = theme === 'paper' ? '🌙 黑板' : '📄 白纸';
  return (
    <header className="topbar">
      <div className="topbar-title">📖 {title}</div>
      {streaming && statusText && (
        <div className="topbar-status">{statusText}<span className="dots" /></div>
      )}
      <div className="topbar-actions">
        <button className="hbtn" onClick={() => setTheme(toggleTheme())}>{label}</button>
        <button className="hbtn" onClick={onOpenSettings}>⚙️ 设置</button>
      </div>
    </header>
  );
}
```

- [ ] **Step 2: 追加顶栏样式到 `web/src/styles.css`**

```css
/* ===== 顶栏 ===== */
.topbar {
  display: flex; align-items: center; gap: 16px;
  padding: 12px 20px;
  border-bottom: 2px solid var(--line);
  background: var(--panel);
}
.topbar-title { font-size: 22px; font-weight: 700; }
.topbar-status { color: var(--accent-2); display: flex; align-items: center; gap: 6px; }
.topbar-actions { margin-left: auto; display: flex; gap: 10px; }
.dots::after {
  content: '…'; display: inline-block; width: 1.2em;
  animation: dots 1.2s steps(4, end) infinite; overflow: hidden; vertical-align: bottom;
}
@keyframes dots { from { width: 0 } to { width: 1.2em } }
```

- [ ] **Step 3: 类型检查 + 构建**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` 零错误，构建成功。（此时 TopBar 尚未被引用——只要文件本身类型正确即可通过；App 接线在 Task 7。）

- [ ] **Step 4: 提交**

```bash
git add web/src/components/TopBar.tsx web/src/styles.css
git commit -m "feat(ui): 顶栏 TopBar（书名+主题切换+设置入口+生成阶段文案）"
```

---

### Task 5: 重构侧边栏 + 主区（手绘树 / 禁用态 / 骨架 / 稿纸 / 流式光标）

重构 `Sidebar` 与 `MainPanel`：手绘卡片树、生成时禁用切换、稿纸阅读区、流式光标、回退按钮移到卡片右上、空状态引导。保持既有 props 契约，仅新增必要 props。

**Files:**
- Rewrite: `web/src/components/Sidebar.tsx`
- Rewrite: `web/src/components/MainPanel.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `BookTree`/`Selection`/`findChapter`（既有）、`CoreSettings`（既有）。
- Produces（新增/变更 props，供 Task 7 的 App 使用）：
  - `Sidebar` 新增 prop：`disabled: boolean`（生成态锁定导航；为 true 时导航项与所有按钮不可点）。其余既有 props 不变：`tree, selection, onSelect, onAddSection, onAddChapter, onOpenSettings, onPlanSections`。
    - 注意：`onOpenSettings` 保留在 props 里但侧栏内**不再渲染设置入口**（迁到 TopBar）。Task 7 调用 Sidebar 时不再传 `onOpenSettings`——因此本任务要把 `onOpenSettings` 从 Sidebar 的 props 中**移除**，避免 `noUnusedParameters` 报错。
  - `MainPanel` 既有 props 不变：`tree, selection, streamingText, streaming, onSaveChapter, onRollback, onSaveCore`。

- [ ] **Step 1: 重写 `web/src/components/Sidebar.tsx`**

移除 `onOpenSettings`（迁至 TopBar），新增 `disabled`；导航项在 `disabled` 时加 `disabled` 类并阻断点击。

```tsx
import type { BookTree } from '../types';
import type { Selection } from '../store';

export function Sidebar({ tree, selection, disabled, onSelect, onAddSection, onAddChapter, onPlanSections }: {
  tree: BookTree; selection: Selection; disabled: boolean;
  onSelect: (s: Selection) => void;
  onAddSection: () => void;
  onAddChapter: (sectionId: string) => void;
  onPlanSections: () => void;
}) {
  const active = (s: Selection) => JSON.stringify(s) === JSON.stringify(selection) ? 'active' : '';
  const go = (s: Selection) => { if (!disabled) onSelect(s); };
  const cls = (s: Selection) => `nav-item ${active(s)} ${disabled ? 'disabled' : ''}`;
  return (
    <aside className={`sidebar sketch ${disabled ? 'locked' : ''}`}>
      <div className="side-tabs">
        <div className={`side-tab ${active({ kind: 'outline' })}`} onClick={() => go({ kind: 'outline' })}>📜 全书大纲</div>
        <div className={`side-tab ${active({ kind: 'core' })}`} onClick={() => go({ kind: 'core' })}>🧭 核心设定</div>
      </div>
      {tree.sections.map((s) => (
        <div key={s.id} className="section">
          <div className="section-title">{s.title}</div>
          <div className="chapter-list">
            {s.chapters.map((c) => (
              <div key={c.id}
                className={`${cls({ kind: 'chapter', sectionId: s.id, chapterId: c.id })} chapter`}
                onClick={() => go({ kind: 'chapter', sectionId: s.id, chapterId: c.id })}>
                <span>{c.title}</span>{c.content ? <span className="done">✓</span> : null}
              </div>
            ))}
          </div>
          <button className="hbtn dashed mini" disabled={disabled} onClick={() => onAddChapter(s.id)}>＋ 加章</button>
        </div>
      ))}
      <div className="side-add">
        <button className="hbtn dashed mini" disabled={disabled} onClick={onAddSection}>＋ 新建部</button>
        <button className="hbtn dashed mini" disabled={disabled} onClick={onPlanSections}>🧩 AI 规划分部</button>
      </div>
    </aside>
  );
}
```

- [ ] **Step 2: 重写 `web/src/components/MainPanel.tsx`**

稿纸卡片 + 流式光标（大纲与章节均支持）+ 回退按钮移右上 + 空状态引导。`CoreEditor` 套手绘表单类。

```tsx
import { useEffect, useState } from 'react';
import type { BookTree, CoreSettings } from '../types';
import type { Selection } from '../store';
import { findChapter } from '../store';

export function MainPanel({ tree, selection, streamingText, streaming, onSaveChapter, onRollback, onSaveCore }: {
  tree: BookTree; selection: Selection;
  streamingText: string; streaming: boolean;
  onSaveChapter: (content: string) => void;
  onRollback: () => void;
  onSaveCore: (core: CoreSettings) => void;
}) {
  const chapter = findChapter(tree, selection);
  const [draft, setDraft] = useState('');
  useEffect(() => { setDraft(chapter?.content ?? ''); }, [chapter?.id, chapter?.content]);

  if (selection.kind === 'core') return <CoreEditor core={tree.book.settings.core} onSave={onSaveCore} />;

  if (selection.kind === 'outline') {
    const body = streaming ? streamingText : tree.book.outline.content;
    return (
      <main className="main">
        <article className="paper sketch">
          <h2 className="paper-title">全书大纲</h2>
          {body
            ? <pre>{body}{streaming && <span className="cursor">▎</span>}</pre>
            : <div className="empty-hint">尚未生成。点下方 <b>🔄 重写</b> 让 AI 起草全书大纲。</div>}
        </article>
      </main>
    );
  }

  // chapter
  if (!chapter) {
    return (
      <main className="main">
        <div className="empty-hint big">
          还没有章节。点左侧 <b>＋ 新建部</b> 或 <b>🧩 AI 规划分部</b> 开始，再 <b>＋ 加章</b>。
        </div>
      </main>
    );
  }
  const text = streaming ? streamingText : draft;
  return (
    <main className="main">
      <article className="paper sketch">
        <div className="paper-head">
          <h2 className="paper-title">{chapter.title}</h2>
          {chapter.history.length > 0 && !streaming &&
            <button className="hbtn mini" onClick={onRollback}>↩ 上一版</button>}
        </div>
        {streaming
          ? <pre>{text}<span className="cursor">▎</span></pre>
          : <textarea className="editor" value={text}
              onChange={(e) => setDraft(e.target.value)}
              onBlur={() => { if (draft !== chapter.content) onSaveChapter(draft); }} />}
      </article>
    </main>
  );
}

function CoreEditor({ core, onSave }: { core: CoreSettings; onSave: (core: CoreSettings) => void }) {
  const [world, setWorld] = useState('');
  const [style, setStyle] = useState('');
  const [constraints, setConstraints] = useState('');
  const [pacing, setPacing] = useState('');
  useEffect(() => {
    setWorld(core?.world ?? '');
    setStyle(core?.style ?? '');
    setConstraints(core?.constraints ?? '');
    setPacing(core?.pacing ?? '');
  }, [core?.world, core?.style, core?.constraints, core?.pacing]);

  return (
    <main className="main">
      <article className="paper sketch">
        <h2 className="paper-title">核心设定</h2>
        <div className="core-form">
          <label>世界观<textarea value={world} onChange={(e) => setWorld(e.target.value)} rows={4} /></label>
          <label>文风基调<textarea value={style} onChange={(e) => setStyle(e.target.value)} rows={3} /></label>
          <label>禁忌约束<textarea value={constraints} onChange={(e) => setConstraints(e.target.value)} rows={3} /></label>
          <label>篇幅节奏<textarea value={pacing} onChange={(e) => setPacing(e.target.value)} rows={3} /></label>
          <button className="hbtn accent-2" onClick={() => onSave({ world, style, constraints, pacing })}>保存设定</button>
        </div>
      </article>
    </main>
  );
}
```

- [ ] **Step 3: 追加侧栏 + 主区样式到 `web/src/styles.css`**

```css
/* ===== 布局骨架 ===== */
.app { display: flex; flex-direction: column; height: 100%; }
.body { flex: 1; display: flex; min-height: 0; }
.content { flex: 1; display: flex; flex-direction: column; min-width: 0; }

/* ===== 侧边栏 ===== */
.sidebar {
  width: 260px; flex: none; margin: 16px 0 16px 16px; padding: 16px;
  overflow: auto; display: flex; flex-direction: column; gap: 14px;
}
.sidebar.locked { opacity: .8; }
.side-tabs { display: flex; flex-direction: column; gap: 8px; }
.side-tab {
  padding: 8px 12px; border: 2px solid var(--line); border-radius: 12px 200px 12px 200px / 200px 12px 200px 12px;
  cursor: pointer; font-weight: 700;
}
.side-tab.active { border-color: var(--accent); color: var(--accent); }
.section-title { font-weight: 700; margin: 6px 0 4px; border-bottom: 2px dashed var(--line); padding-bottom: 2px; }
.chapter-list { display: flex; flex-direction: column; gap: 2px; }
.nav-item { padding: 5px 8px; cursor: pointer; border-radius: 6px; display: flex; gap: 6px; align-items: center; }
.nav-item:hover { background: color-mix(in srgb, var(--line) 10%, transparent); }
.nav-item.active { border: 2px solid var(--accent); border-radius: 200px 12px 200px 12px / 12px 200px 12px 200px; }
.nav-item.disabled { opacity: .5; cursor: not-allowed; }
.nav-item .done { color: var(--accent-2); margin-left: auto; }
.side-add { display: flex; flex-direction: column; gap: 8px; margin-top: auto; }
.mini { padding: 5px 10px; font-size: 14px; }

/* ===== 主区 / 稿纸 ===== */
.main { flex: 1; overflow: auto; padding: 16px; }
.paper { padding: 24px 28px; max-width: 820px; margin: 0 auto; min-height: 60vh; }
.paper-head { display: flex; align-items: center; gap: 12px; }
.paper-title {
  margin: 0 0 12px; font-size: 26px;
  border-bottom: 3px solid var(--accent); display: inline-block; padding-bottom: 4px;
}
.paper pre { font-size: 17px; line-height: 1.9; }
.editor {
  width: 100%; min-height: 52vh; border: none; outline: none;
  background: transparent; resize: vertical; font-size: 17px; line-height: 1.9;
}
.editor:focus { outline: 2px dashed var(--line); outline-offset: 6px; border-radius: 8px; }
.empty-hint { color: var(--ink-soft); padding: 20px 0; }
.empty-hint.big { text-align: center; margin: 12vh auto; max-width: 480px; font-size: 18px; }

/* ===== 核心设定表单 ===== */
.core-form { display: flex; flex-direction: column; gap: 14px; }
.core-form label { display: flex; flex-direction: column; gap: 6px; font-weight: 700; }
.core-form textarea {
  width: 100%; padding: 10px; border: 2px solid var(--line);
  border-radius: 12px; background: var(--bg); resize: vertical; font-weight: 400;
}
.core-form button { align-self: flex-start; }
```

- [ ] **Step 4: 类型检查 + 构建**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` **会报错**——因为 `App.tsx` 仍向 `Sidebar` 传 `onOpenSettings` 且未传 `disabled`。这是预期的临时不一致，将在 Task 7 修复。

为了让本任务可独立验证类型，先只对改动的两个文件做隔离检查：确认 `Sidebar.tsx`、`MainPanel.tsx` 本身无语法/类型错误（通过通读代码确认导入均被使用、无未用参数）。完整 `tsc -b`/构建的绿灯在 Task 7 达成。

说明：`App.tsx` 的适配属于 Task 7 的接线职责，本任务不改 `App.tsx`，以保持任务边界清晰。审查者据此判断本任务交付物（两个组件 + 样式）是否合格。

- [ ] **Step 5: 提交**

```bash
git add web/src/components/Sidebar.tsx web/src/components/MainPanel.tsx web/src/styles.css
git commit -m "feat(ui): 重构侧栏（手绘树/禁用态）与主区（稿纸/流式光标/空状态）"
```

---

### Task 6: 重构动作条 + 设置页 + 开篇页（手绘按钮 / 停止态 / 手绘表单）

重构 `Actions`（手绘按钮、朱砂红「抽」、脉冲停止态）、`SettingsPage`（手绘卡片表单）、`FirstRun`（手绘开篇）。props 契约不变。

**Files:**
- Rewrite: `web/src/components/Actions.tsx`
- Rewrite: `web/src/components/SettingsPage.tsx`
- Rewrite: `web/src/components/FirstRun.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `Config`（既有）、`getConfig`/`saveConfig`/`createBook`（既有 api）、`Book`（既有）。
- Produces（props 全部不变）：
  - `Actions`：`streaming, onRewrite, onNext, onWhip, onStop`
  - `SettingsPage`：`onClose`
  - `FirstRun`：`onCreated`

- [ ] **Step 1: 重写 `web/src/components/Actions.tsx`**

```tsx
import { useState } from 'react';

export function Actions({ streaming, onRewrite, onNext, onWhip, onStop }: {
  streaming: boolean;
  onRewrite: () => void;
  onNext: () => void;
  onWhip: (text: string) => void;
  onStop: () => void;
}) {
  const [whip, setWhip] = useState('');
  return (
    <div className="actions sketch">
      {streaming ? (
        <button className="hbtn stop" onClick={onStop}>⏹ 停止</button>
      ) : (
        <>
          <div className="btn-row">
            <button className="hbtn" onClick={onRewrite}>🔄 重写</button>
            <button className="hbtn accent-2" onClick={onNext}>➡️ 下一章</button>
          </div>
          <div className="whip-row">
            <textarea className="whip-input" placeholder="狠狠抽打：写下你的不满与要求…" value={whip}
              onChange={(e) => setWhip(e.target.value)} />
            <button className="hbtn accent whip-btn" onClick={() => { if (whip.trim()) { onWhip(whip); setWhip(''); } }}>🗯️ 抽</button>
          </div>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 重写 `web/src/components/SettingsPage.tsx`**

```tsx
import { useEffect, useState } from 'react';
import type { Config } from '../types';
import { getConfig, saveConfig } from '../api';

export function SettingsPage({ onClose }: { onClose: () => void }) {
  const [cfg, setCfg] = useState<Config>({ baseUrl: '', model: '', apiKey: '', chapterWordTarget: 2000 });
  useEffect(() => { getConfig().then(setCfg); }, []);
  const set = (k: keyof Config) => (e: React.ChangeEvent<HTMLInputElement>) =>
    setCfg({ ...cfg, [k]: k === 'chapterWordTarget' ? Number(e.target.value) : e.target.value });
  return (
    <div className="settings-page">
      <article className="paper sketch">
        <h2 className="paper-title">API 设置</h2>
        <div className="core-form">
          <label>Base URL<input value={cfg.baseUrl} onChange={set('baseUrl')} placeholder="https://api.deepseek.com/v1" /></label>
          <label>模型<input value={cfg.model} onChange={set('model')} placeholder="deepseek-chat" /></label>
          <label>API Key<input value={cfg.apiKey} onChange={set('apiKey')} placeholder="sk-..." /></label>
          <label>每章目标字数<input type="number" value={cfg.chapterWordTarget} onChange={set('chapterWordTarget')} /></label>
          <div className="btn-row">
            <button className="hbtn accent-2" onClick={async () => { const s = await saveConfig(cfg); setCfg(s); onClose(); }}>保存</button>
            <button className="hbtn" onClick={onClose}>返回</button>
          </div>
        </div>
      </article>
    </div>
  );
}
```

- [ ] **Step 3: 重写 `web/src/components/FirstRun.tsx`**

```tsx
import { useState } from 'react';
import { createBook } from '../api';
import type { Book } from '../types';

export function FirstRun({ onCreated }: { onCreated: (book: Book) => void }) {
  const [premise, setPremise] = useState('');
  const [busy, setBusy] = useState(false);
  return (
    <div className="first-run">
      <article className="paper sketch first-card">
        <h1 className="paper-title">📖 自动小说盒子</h1>
        <p className="first-lead">用一句话，开始你的小说。你只管挥鞭子。</p>
        <textarea className="first-input" value={premise} onChange={(e) => setPremise(e.target.value)}
          placeholder="例如：写一个赛博朋克风的侦探故事" />
        <button className="hbtn accent" disabled={!premise.trim() || busy}
          onClick={async () => { setBusy(true); onCreated(await createBook(premise)); }}>
          {busy ? '正在创建…' : '开始 ✍️'}
        </button>
      </article>
    </div>
  );
}
```

- [ ] **Step 4: 追加动作条/设置/开篇样式到 `web/src/styles.css`**

```css
/* ===== 底部动作条 ===== */
.actions { margin: 0 16px 16px; padding: 14px 16px; display: flex; flex-direction: column; gap: 12px; }
.btn-row { display: flex; gap: 12px; }
.whip-row { display: flex; gap: 12px; align-items: stretch; }
.whip-input {
  flex: 1; min-height: 52px; padding: 10px; resize: vertical;
  border: 2px solid var(--line); border-radius: 12px; background: var(--bg);
}
.whip-input:focus { outline: none; border-color: var(--accent); }
.whip-btn { font-size: 18px; padding: 8px 22px; }
.stop {
  width: 100%; border-color: var(--accent); color: var(--accent);
  animation: pulse 1.2s ease-in-out infinite;
}
@keyframes pulse {
  0%,100% { box-shadow: 2px 2px 0 var(--shadow); }
  50% { box-shadow: 0 0 0 4px color-mix(in srgb, var(--accent) 30%, transparent); }
}

/* ===== 设置页 / 开篇页 ===== */
.settings-page { max-width: 720px; margin: 32px auto; padding: 0 16px; }
.settings-page input {
  padding: 10px; border: 2px solid var(--line); border-radius: 12px;
  background: var(--bg); font-weight: 400;
}
.first-run { display: flex; align-items: center; justify-content: center; height: 100%; padding: 24px; }
.first-card { max-width: 560px; width: 100%; text-align: center; display: flex; flex-direction: column; gap: 16px; }
.first-lead { color: var(--ink-soft); margin: 0; }
.first-input {
  min-height: 96px; padding: 12px; resize: vertical;
  border: 2px solid var(--line); border-radius: 12px; background: var(--bg);
}
.first-card .hbtn { align-self: center; padding: 10px 28px; font-size: 18px; }
```

- [ ] **Step 5: 类型检查（隔离通读）**

同 Task 5：本任务只改这 3 个组件，`App.tsx` 尚未接线，完整 `tsc -b` 的绿灯在 Task 7。通读确认三个文件导入均被使用、无未用参数、props 类型与既有 App 调用兼容（`Actions`/`SettingsPage`/`FirstRun` 的 props 未变，故它们对 App 是向后兼容的）。

- [ ] **Step 6: 提交**

```bash
git add web/src/components/Actions.tsx web/src/components/SettingsPage.tsx web/src/components/FirstRun.tsx web/src/styles.css
git commit -m "feat(ui): 重构动作条（手绘按钮/朱砂抽打/脉冲停止）与设置/开篇页"
```

---

### Task 7: App.tsx 集成（接线全部 + alert→toast + 生成态锁定 + 阶段文案 + 完成反馈 + 加载骨架 + 响应式收尾）

把 TopBar、Toast、SectionPlanPanel 全部接入；用 toast 替换全部 `alert`；生成时锁定侧栏；顶栏显示阶段文案；生成完成发成功 toast；启动加载期显示骨架避免闪 FirstRun；补窄屏折叠样式。这是让全部前置任务真正生效的收口任务，交付后 `tsc -b`/`npm run build`/`npm test` 全绿。

**Files:**
- Rewrite: `web/src/App.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes:
  - `TopBar`（Task 4）：`{ title, streaming, statusText, onOpenSettings }`
  - `useToast`（Task 2）：`{ success, error, info }`
  - `SectionPlanPanel`（Task 3）：`{ text, streaming, onAdopt, onClose }`
  - `parseSectionTitles` 已在面板内部使用；App 通过 `onAdopt(titles)` 收到解析结果。
  - `Sidebar`（Task 5）：新增必传 `disabled`，不再传 `onOpenSettings`。
  - 既有 api：`api.addSection(bookId, title?)`、`api.getTree`、`api.streamGen` 等。

- [ ] **Step 1: 重写 `web/src/App.tsx`**

```tsx
import { useEffect, useRef, useState } from 'react';
import type { Book, BookTree } from './types';
import type { Selection } from './store';
import { firstSelectable } from './store';
import * as api from './api';
import { useToast } from './components/Toast';
import { TopBar } from './components/TopBar';
import { Sidebar } from './components/Sidebar';
import { MainPanel } from './components/MainPanel';
import { Actions } from './components/Actions';
import { SettingsPage } from './components/SettingsPage';
import { FirstRun } from './components/FirstRun';
import { SectionPlanPanel } from './components/SectionPlanPanel';

export default function App() {
  const toast = useToast();
  const [loading, setLoading] = useState(true);
  const [tree, setTree] = useState<BookTree | null>(null);
  const [selection, setSelection] = useState<Selection>({ kind: 'outline' });
  const [streaming, setStreaming] = useState(false);
  const [streamingText, setStreamingText] = useState('');
  const [statusText, setStatusText] = useState('');
  const [showSettings, setShowSettings] = useState(false);
  const [planOpen, setPlanOpen] = useState(false);
  const [planText, setPlanText] = useState('');
  const abortRef = useRef<null | (() => void)>(null);

  useEffect(() => {
    api.listBooks().then(async (list) => {
      if (list.length) { const t = await api.getTree(list[0].id); setTree(t); setSelection(firstSelectable(t)); }
    }).finally(() => setLoading(false));
  }, []);

  const reload = async (bookId: string, sel?: Selection) => {
    const t = await api.getTree(bookId);
    setTree(t);
    if (sel) setSelection(sel);
  };

  if (showSettings) return <SettingsPage onClose={() => setShowSettings(false)} />;
  if (loading) return <div className="boot-skeleton"><div className="sk-line" /><div className="sk-line" /><div className="sk-line short" /></div>;
  if (!tree) return <FirstRun onCreated={async (b: Book) => { await reload(b.id, { kind: 'outline' }); }} />;

  const bookId = tree.book.id;

  const runChapter = (mode: 'next' | 'rewrite' | 'whip', whip?: string) => {
    const sel = selection;
    const sectionId = sel.kind === 'chapter' ? sel.sectionId : tree.sections[0]?.id;
    if (!sectionId) { toast.error('请先新建一个部'); return; }
    const chapterId = sel.kind === 'chapter' ? sel.chapterId : undefined;
    const stage = mode === 'whip' ? '🗯️ 正在按你的要求重写…' : mode === 'rewrite' ? '✍️ 正在重写本章…' : '✍️ 正在写下一章…';
    setStreaming(true); setStreamingText(''); setStatusText(stage);
    abortRef.current = api.streamGen('/api/gen/chapter',
      { bookId, sectionId, chapterId, mode, whip },
      {
        onDelta: (d) => setStreamingText((t) => t + d),
        onError: (m) => { setStreaming(false); setStatusText(''); toast.error('生成失败：' + m); },
        onDone: async (e) => {
          setStreaming(false); setStatusText('');
          await reload(bookId, { kind: 'chapter', sectionId, chapterId: e.chapterId ?? chapterId! });
          toast.success('✓ 本章完成');
        },
      });
  };

  const runOutline = () => {
    setStreaming(true); setStreamingText(''); setStatusText('✍️ 正在构思全书大纲…');
    abortRef.current = api.streamGen('/api/gen/outline', { bookId }, {
      onDelta: (d) => setStreamingText((t) => t + d),
      onError: (m) => { setStreaming(false); setStatusText(''); toast.error('生成失败：' + m); },
      onDone: async () => { setStreaming(false); setStatusText(''); await reload(bookId, { kind: 'outline' }); toast.success('✓ 大纲已生成'); },
    });
  };

  const runSections = () => {
    setPlanOpen(true); setPlanText('');
    setStreaming(true); setStatusText('🧩 正在规划分部…');
    abortRef.current = api.streamGen('/api/gen/sections', { bookId }, {
      onDelta: (d) => setPlanText((t) => t + d),
      onError: (m) => { setStreaming(false); setStatusText(''); toast.error('规划失败：' + m); },
      onDone: (e) => { setStreaming(false); setStatusText(''); if (e.sections) setPlanText(e.sections); },
    });
  };

  const adoptSections = async (titles: string[]) => {
    setPlanOpen(false);
    for (const t of titles) await api.addSection(bookId, t);
    await reload(bookId);
    toast.success(`✓ 已创建 ${titles.length} 个部`);
  };

  const stopGen = () => { abortRef.current?.(); setStreaming(false); setStatusText(''); };

  return (
    <div className="app">
      <TopBar title={tree.book.title} streaming={streaming} statusText={statusText}
        onOpenSettings={() => setShowSettings(true)} />
      <div className="body">
        <Sidebar tree={tree} selection={selection} disabled={streaming}
          onSelect={setSelection}
          onAddSection={async () => { await api.addSection(bookId); await reload(bookId); }}
          onAddChapter={async (sid) => { const c = await api.addChapter(bookId, sid); await reload(bookId, { kind: 'chapter', sectionId: sid, chapterId: c.id }); }}
          onPlanSections={runSections} />
        <div className="content">
          <MainPanel tree={tree} selection={selection} streaming={streaming} streamingText={streamingText}
            onSaveChapter={async (content) => {
              if (selection.kind === 'chapter') { await api.saveChapter(bookId, selection.sectionId, selection.chapterId, content); await reload(bookId); toast.success('✓ 已保存'); }
            }}
            onRollback={async () => {
              if (selection.kind === 'chapter') { await api.rollbackChapter(bookId, selection.sectionId, selection.chapterId); await reload(bookId); toast.info('已回到上一版'); }
            }}
            onSaveCore={async (core) => { await api.saveCore(bookId, core); await reload(bookId); toast.success('✓ 设定已保存'); }} />
          <Actions streaming={streaming}
            onRewrite={() => {
              if (selection.kind === 'outline') runOutline();
              else if (selection.kind === 'chapter') runChapter('rewrite');
            }}
            onNext={() => runChapter('next')}
            onWhip={(t) => runChapter('whip', t)}
            onStop={stopGen} />
        </div>
      </div>
      {planOpen && <SectionPlanPanel text={planText} streaming={streaming}
        onAdopt={adoptSections} onClose={() => { if (streaming) stopGen(); setPlanOpen(false); }} />}
    </div>
  );
}
```

- [ ] **Step 2: 追加骨架 + 响应式样式到 `web/src/styles.css`**

```css
/* ===== 启动骨架 ===== */
.boot-skeleton { padding: 48px; display: flex; flex-direction: column; gap: 16px; max-width: 480px; margin: 10vh auto; }
.sk-line { height: 18px; border-radius: 6px; background: color-mix(in srgb, var(--line) 15%, transparent); animation: sk 1.2s ease-in-out infinite; }
.sk-line.short { width: 60%; }
@keyframes sk { 0%,100% { opacity: .5 } 50% { opacity: 1 } }

/* ===== 响应式（窄屏） ===== */
@media (max-width: 720px) {
  .body { flex-direction: column; }
  .sidebar { width: auto; margin: 12px; max-height: 220px; }
  .paper { padding: 16px; }
}
```

- [ ] **Step 3: 运行全部测试**

Run（cwd=`web`）: `npm test`
Expected: PASS —— `theme`(3) + `toast`(3) + `sections`(5) + `store`(4) + `api`(既有) 全绿。

- [ ] **Step 4: 类型检查 + 构建（完整绿灯）**

Run（cwd=`web`）: `npm run build`
Expected: `tsc -b` 零错误（此时 Sidebar 不再收 `onOpenSettings`、且收到 `disabled`，与 App 一致），vite 产出 `dist` 成功。

- [ ] **Step 5: 端到端手动验收（对照 spec §7）**

启动应用（项目根 `启动.command` 或 `web` 目录 `npm run dev`），逐项确认：
- 顶栏主题切换：白纸 ↔ 黑板即时生效，刷新后保持（localStorage）。
- 生成大纲：正文区出现流式光标 `▎`，顶栏显示「✍️ 正在构思全书大纲…」+ 跑马灯点，侧栏变灰不可点，完成后 toast「✓ 大纲已生成」。
- AI 规划分部：弹出手绘面板、流式显示建议，停流后底部列出「将创建 N 个部」，点「✓ 采纳并创建这些部」→ 面板关闭 + toast「✓ 已创建 N 个部」+ 侧栏出现这些部。
- 写章 / 下一章 / 抽打：阶段文案正确，完成 toast「✓ 本章完成」。
- 回退：章节 history 非空时右上出现「↩ 上一版」，点后 toast「已回到上一版」。
- 触发一次错误（如清空 API Key 后生成）：出现红色 error toast，停留更久、可手动 ×，**全程无浏览器 alert 弹窗**。
- 窄屏（<720px）：侧栏转为顶部横条，主区可读。

- [ ] **Step 6: 确认已无残留 alert**

Run（cwd=`web`）: `grep -rn "alert(" src` —— Expected: 无输出（所有 alert 已被 toast 取代）。

- [ ] **Step 7: 提交**

```bash
git add web/src/App.tsx web/src/styles.css
git commit -m "feat(ui): App 集成 TopBar/Toast/分部面板，生成态锁定+阶段文案+完成反馈，alert 全替换为 toast"
```

---

## Self-Review

**1. Spec coverage（逐节核对）：**
- §2 主题系统与基元 → Task 1（theme.ts + 变量 + .sketch + 字体 + 光标 + main 应用）。✅
- §3 三大区域布局：§3.1 顶栏 → Task 4；§3.2 侧栏 → Task 5；§3.3 主区稿纸 → Task 5；§3.4 动作条 → Task 6；§3.5 响应式 → Task 7 Step 2。✅
- §4 生成状态反馈：§4.1 流式光标 → Task 5（MainPanel）+ Task 3（面板）；§4.2 生成态锁定 → Task 7（`disabled=streaming`）+ Task 6（停止态 pulse）；§4.3 阶段文案 → Task 7（statusText）；§4.4 完成轻反馈 toast → Task 7；§4.5 骨架/空状态 → Task 7（boot-skeleton）+ Task 5（empty-hint）。✅
- §5 Toast：§5.1 自研 toast → Task 2；§5.2 分部面板 → Task 3 + Task 7 接线；§5.3 错误统一 toast → Task 7；§5.4 无确认弹窗 → 无需实现。✅
- §6 组件改造清单：TopBar(T4)/Toast(T2)/SectionPlanPanel(T3)/theme.ts(T1)/Sidebar+MainPanel(T5)/Actions+Settings+FirstRun(T6)/App(T7)/styles.css(全程)/fonts(T1)。✅ 全覆盖。
- §7 测试策略：theme 纯函数(T1)、分部解析(T3)、toast reducer(T2 额外)、既有测试全绿(T7)、tsc -b(各任务)、build(T7)、端到端手动(T7 Step 5)。✅
- §8 YAGNI：无新依赖、无 rough.js、无快捷键/引导、不改后端、窄屏仅基础可用、无删除确认。✅

**2. Placeholder scan：** 所有步骤含真实代码/命令；字体下载步骤显式声明失败可跳过（非占位，是有意的降级路径）；无 TBD/TODO/「类似 Task N」。✅

**3. Type consistency：**
- `Theme` 类型贯穿 theme.ts / TopBar 一致。
- `ToastApi` 的 `success/error/info` 在 Toast.tsx 定义、App 调用一致。
- `SectionPlanPanel` props `{text, streaming, onAdopt, onClose}` 在 Task 3 定义、Task 7 调用一致。
- `Sidebar` props 变更（去 `onOpenSettings`、加 `disabled`）在 Task 5 定义、Task 7 调用一致；Task 5 Step 4 明确说明该变更导致的临时 tsc 不一致由 Task 7 收口——这是有意的任务边界，不是 bug。
- `TopBar` props `{title, streaming, statusText, onOpenSettings}` Task 4 定义、Task 7 调用一致。
- `parseSectionTitles(text): string[]` Task 3 定义、面板内部调用一致。
- `api.addSection(bookId, title?)` 既有签名，Task 7 `adoptSections` 传 title 用法与既有契约一致。✅

**关于任务顺序与临时红灯：** Task 5/6 交付后 `tsc -b` 会因 App 未接线而暂时报错；这是刻意的——把「改组件」与「改 App 接线」分成可独立审查的任务。审查 Task 5/6 时以「组件文件自身类型/逻辑正确 + 样式合理」为准；完整构建绿灯在 Task 7 达成并强制验证。执行者须知晓此约定，勿在 Task 5/6 误判为失败。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-30-UI交互优化.md`.
