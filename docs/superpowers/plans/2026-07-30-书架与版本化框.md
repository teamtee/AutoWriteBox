# 书架 + 统一版本化框 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 新增类微信读书的书架（多书管理：新建/进入/重命名/删除/概览），并把大纲、章节正文、核心设定四子框统一为「可版本化字段」`{versions,cursor}`，每框自带行内工具条 `◀上一个 · 🔄重写 · ▶下一个 · 🗑删除`（双向浏览），同时把写作动作上下文化（大纲/核心页不再出现章节动作）。

**Architecture:** 后端先落地——统一版本化原语（纯函数）+ 惰性迁移老书 + 统一版本端点 + 书架 CRUD；前端后落地——通用 `VersionedBox` 组件 + `Bookshelf` 页 + MainPanel/Actions/App 重构。后端 9 任务里前 5 个是后端，后 4 个是前端。

**Tech Stack:** 后端 Node ESM + Express，测试 `node --test`；前端 React 18 + Vite + TS(strict/noUnusedLocals/noUnusedParameters)，测试 vitest。

## Global Constraints

- **不新增任何 npm 依赖**（后端仅 express；前端仅现有依赖）。
- **原子写**：所有落盘走 `atomicWriteJson`（临时文件 + rename）。
- **路径安全**：所有外部传入的 id/path 过 `safeId` 白名单或 `parseVersionPath` 白名单，非法一律 400/抛 BAD_ID/BAD_PATH。
- **版本上限**：沿用 `HISTORY_MAX = 20`，超限从最旧端 shift 且 cursor 相应向左收敛（不小于 0）。
- **派生一致性**：章节 `content` 是 `currentText(body)` 的派生只读值，任何写 `body` 的路径落盘前必须同步 `content`（集中在 `writeChapter` 内）。
- **后端测试**：`node --test server/test/*.test.js` 全绿。
- **前端**：`tsc -b` 零错误（noUnusedLocals/noUnusedParameters）；`npm run build`（cd web）成功；`npm test`（cd web，vitest）全绿。
- **颜色全走 CSS 变量**（书架/工具条/版本指示新样式）。中文注释、简短。
- 后端命令在仓库根执行；前端命令在 `web/` 执行（cwd=web）。

## 数据模型（落盘格式，全任务共享）

**book.json**（迁移后）：
```json
{
  "id":"...", "title":"...", "createdAt":"...", "updatedAt":"...", "premise":"...",
  "outline": { "versions": ["v1","v2"], "cursor": 1 },
  "settings": { "core": {
      "world":       { "versions": ["..."], "cursor": 0 },
      "style":       { "versions": ["..."], "cursor": 0 },
      "constraints": { "versions": ["..."], "cursor": 0 },
      "pacing":      { "versions": ["..."], "cursor": 0 }
    }, "history": [] },
  "characters": [], "summary": "", "progress": "", "sections": ["section-01"]
}
```
**chapter.json**（迁移后）：
```json
{ "id":"chapter-01", "index":1, "title":"...",
  "body": { "versions": ["正文v1"], "cursor": 0 },
  "content": "正文v1",            // 派生只读 = currentText(body)
  "characters": [], "summary": "", "progress": "", "status": "done" }
```
**section.json**：保持不变（`outline:{content,history}` 不做版本化——当前 UI 不编辑本部大纲，YAGNI）。
**version path 白名单**：`outline` | `core:world|style|constraints|pacing` | `section:<sid>:chapter:<cid>`。

---

### Task 1: 版本化原语 + path 解析 + 迁移（后端纯函数）

**Files:**
- Modify: `server/store.js`（新增纯函数导出，不改现有函数）
- Test: `server/test/versioned.test.js`（新建）

**Interfaces:**
- Produces（均从 `server/store.js` 导出，纯函数无 I/O）：
  - `emptyVersioned(): {versions:[''],cursor:0}`
  - `currentText(vf): string`
  - `commitVersion(vf, text): vf`（push 新版、cursor 指末尾、超 HISTORY_MAX 从头 shift 且 cursor 收敛）
  - `moveCursor(vf, delta): boolean`（越界返回 false 且不改）
  - `migrateVersioned(old): {versions,cursor}`（新结构原样 / 字符串→单版 / `{content,history}`→合并 / 空→emptyVersioned）
  - `parseVersionPath(path): {type:'outline'} | {type:'core',field} | {type:'chapter',sectionId,chapterId}`（非法抛 `BAD_PATH`）

- [ ] **Step 1: 写失败测试 `server/test/versioned.test.js`**

```js
import { test } from 'node:test';
import assert from 'node:assert/strict';
import {
  emptyVersioned, currentText, commitVersion, moveCursor, migrateVersioned, parseVersionPath,
} from '../store.js';

test('emptyVersioned/currentText', () => {
  const v = emptyVersioned();
  assert.deepEqual(v, { versions: [''], cursor: 0 });
  assert.equal(currentText(v), '');
  assert.equal(currentText({ versions: ['a', 'b'], cursor: 1 }), 'b');
});

test('commitVersion 追加并指向末尾', () => {
  const v = { versions: ['a'], cursor: 0 };
  commitVersion(v, 'b');
  assert.deepEqual(v, { versions: ['a', 'b'], cursor: 1 });
});

test('commitVersion 超 20 上限从头裁剪并收敛 cursor', () => {
  const v = { versions: Array.from({ length: 20 }, (_, i) => String(i)), cursor: 0 };
  commitVersion(v, 'new');
  assert.equal(v.versions.length, 20);
  assert.equal(v.versions[19], 'new');
  assert.equal(v.versions[0], '1');       // 原 '0' 被 shift
  assert.equal(v.cursor, 19);
});

test('moveCursor 边界', () => {
  const v = { versions: ['a', 'b', 'c'], cursor: 1 };
  assert.equal(moveCursor(v, -1), true); assert.equal(v.cursor, 0);
  assert.equal(moveCursor(v, -1), false); assert.equal(v.cursor, 0);
  v.cursor = 2;
  assert.equal(moveCursor(v, 1), false); assert.equal(v.cursor, 2);
});

test('migrateVersioned 各形态', () => {
  assert.deepEqual(migrateVersioned({ versions: ['x'], cursor: 0 }), { versions: ['x'], cursor: 0 });
  assert.deepEqual(migrateVersioned('str'), { versions: ['str'], cursor: 0 });
  assert.deepEqual(migrateVersioned({ content: 'cur', history: ['h1', 'h2'] }),
    { versions: ['h1', 'h2', 'cur'], cursor: 2 });
  assert.deepEqual(migrateVersioned({ content: '', history: [] }), { versions: [''], cursor: 0 });
  assert.deepEqual(migrateVersioned(null), { versions: [''], cursor: 0 });
  assert.deepEqual(migrateVersioned(undefined), { versions: [''], cursor: 0 });
});

test('parseVersionPath 白名单', () => {
  assert.deepEqual(parseVersionPath('outline'), { type: 'outline' });
  assert.deepEqual(parseVersionPath('core:world'), { type: 'core', field: 'world' });
  assert.deepEqual(parseVersionPath('section:section-01:chapter:chapter-02'),
    { type: 'chapter', sectionId: 'section-01', chapterId: 'chapter-02' });
  assert.throws(() => parseVersionPath('core:evil'), /BAD_PATH/);
  assert.throws(() => parseVersionPath('nope'), /BAD_PATH/);
  assert.throws(() => parseVersionPath('section:../x:chapter:y'), /BAD_ID|BAD_PATH/);
  assert.throws(() => parseVersionPath(123), /BAD_PATH/);
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test server/test/versioned.test.js`
Expected: FAIL —— 这些函数尚未导出。

- [ ] **Step 3: 在 `server/store.js` 新增纯函数**（放在 `HISTORY_MAX` 定义之后；`HISTORY_MAX` 现为第 121 行附近的 `const HISTORY_MAX = 20;`）

```js
// ——— 可版本化字段原语（纯函数）———
export function emptyVersioned() { return { versions: [''], cursor: 0 }; }
export function currentText(vf) { return (vf && Array.isArray(vf.versions)) ? (vf.versions[vf.cursor] ?? '') : ''; }
export function commitVersion(vf, text) {
  vf.versions.push(text ?? '');
  while (vf.versions.length > HISTORY_MAX) { vf.versions.shift(); if (vf.cursor > 0) vf.cursor--; }
  vf.cursor = vf.versions.length - 1;
  return vf;
}
export function moveCursor(vf, delta) {
  const n = vf.cursor + delta;
  if (n < 0 || n >= vf.versions.length) return false;
  vf.cursor = n; return true;
}
// 老结构 → 新结构：新结构原样 / 字符串→单版 / {content,history}→合并 / 其它→空
export function migrateVersioned(old) {
  if (old && Array.isArray(old.versions)) return old;
  if (typeof old === 'string') return { versions: [old], cursor: 0 };
  if (old && (typeof old.content === 'string' || Array.isArray(old.history))) {
    const history = Array.isArray(old.history) ? old.history : [];
    const versions = [...history, old.content ?? ''];
    return { versions, cursor: versions.length - 1 };
  }
  return emptyVersioned();
}
// 版本路径解析（白名单，防注入）
export function parseVersionPath(path) {
  if (typeof path !== 'string') throw new Error('BAD_PATH');
  if (path === 'outline') return { type: 'outline' };
  const core = path.match(/^core:(world|style|constraints|pacing)$/);
  if (core) return { type: 'core', field: core[1] };
  const ch = path.match(/^section:([\w-]+):chapter:([\w-]+)$/);
  if (ch) return { type: 'chapter', sectionId: safeId(ch[1]), chapterId: safeId(ch[2]) };
  throw new Error('BAD_PATH');
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test server/test/versioned.test.js`
Expected: PASS。再跑全量 `node --test server/test/*.test.js` 确认无回归（此时旧结构未改，其它测试仍绿）。

- [ ] **Step 5: 提交**

```bash
git add server/store.js server/test/versioned.test.js
git commit -m "feat(store): 可版本化字段原语 + path 解析 + 迁移（纯函数）"
```

---

### Task 2: store I/O 集成（惰性迁移 + 派生 content + 书架 + 版本读写）

**Files:**
- Modify: `server/store.js`
- Test: `server/test/store-versioned-io.test.js`（新建）

**Interfaces:**
- Consumes: Task 1 的 `emptyVersioned/currentText/commitVersion/moveCursor/migrateVersioned/parseVersionPath`。
- Produces（`server/store.js`）：
  - `createBook`：outline/core 四字段用 `emptyVersioned()`。
  - `addChapter`：chapter 带 `body: emptyVersioned()`、`content: ''`。
  - `readBook(id)`：读盘后惰性迁移（outline + core 四字段），返回迁移后对象。
  - `readChapter(...)`：读盘后惰性迁移（生成 `body`，同步派生 `content`）。
  - `writeChapter(...)`：落盘前 `obj.content = currentText(obj.body)`（若有 body）。
  - `listBooks()`：每项加 `sectionCount`、`chapterCount`。
  - `deleteBook(id)`：递归删书目录。
  - `renameBook(id, title)`：改 title 写回，返回更新后 book。
  - `versionMove(bookId, path, delta): Promise<vf>`
  - `versionSet(bookId, path, text): Promise<vf>`（commit 新版 + 落盘；clear 传 `''`，save 传文本）

- [ ] **Step 1: 写失败测试 `server/test/store-versioned-io.test.js`**

```js
import { test, beforeEach } from 'node:test';
import assert from 'node:assert/strict';
import { mkdtempSync, writeFileSync, mkdirSync } from 'node:fs';
import { tmpdir } from 'node:os';
import { join } from 'node:path';
import * as store from '../store.js';

let root;
beforeEach(() => { root = mkdtempSync(join(tmpdir(), 'novelbox-')); store.setDataRoot(root); });

test('createBook 用可版本化字段', async () => {
  const b = await store.createBook({ premise: 'p', title: 't' });
  assert.deepEqual(b.outline, { versions: [''], cursor: 0 });
  assert.deepEqual(b.settings.core.world, { versions: [''], cursor: 0 });
});

test('addChapter 带 body + 派生 content', async () => {
  const b = await store.createBook({ premise: 'p' });
  const s = await store.addSection(b.id, {});
  const c = await store.addChapter(b.id, s.id, {});
  assert.deepEqual(c.body, { versions: [''], cursor: 0 });
  assert.equal(c.content, '');
});

test('readBook 惰性迁移老书', async () => {
  // 手写一份老格式 book.json
  const id = 'book_old_0001';
  mkdirSync(join(root, 'books', id), { recursive: true });
  writeFileSync(join(root, 'books', id, 'book.json'), JSON.stringify({
    id, title: '老书', createdAt: 'x', updatedAt: 'x', premise: 'p',
    outline: { content: '大纲当前', history: ['大纲旧'] },
    settings: { core: { world: '世界观字符串', style: '', constraints: '', pacing: '' }, history: [] },
    characters: [], summary: '', progress: '', sections: [],
  }), 'utf8');
  const b = await store.readBook(id);
  assert.deepEqual(b.outline, { versions: ['大纲旧', '大纲当前'], cursor: 1 });
  assert.deepEqual(b.settings.core.world, { versions: ['世界观字符串'], cursor: 0 });
});

test('readChapter 惰性迁移 + 派生 content', async () => {
  const b = await store.createBook({ premise: 'p' });
  const s = await store.addSection(b.id, {});
  // 手写老格式章节
  const cid = 'chapter-01';
  writeFileSync(join(root, 'books', b.id, s.id, `${cid}.json`), JSON.stringify({
    id: cid, index: 1, title: '第一章', content: '正文当前', history: ['正文旧'],
    characters: [], summary: '', progress: '', status: 'done',
  }), 'utf8');
  const sec = await store.readSection(b.id, s.id); sec.chapters.push(cid); await store.writeSection(b.id, s.id, sec);
  const ch = await store.readChapter(b.id, s.id, cid);
  assert.deepEqual(ch.body, { versions: ['正文旧', '正文当前'], cursor: 1 });
  assert.equal(ch.content, '正文当前');
});

test('listBooks 带部/章计数', async () => {
  const b = await store.createBook({ premise: 'p', title: 'A' });
  const s = await store.addSection(b.id, {});
  await store.addChapter(b.id, s.id, {});
  await store.addChapter(b.id, s.id, {});
  const list = await store.listBooks();
  const row = list.find((x) => x.id === b.id);
  assert.equal(row.sectionCount, 1);
  assert.equal(row.chapterCount, 2);
});

test('deleteBook / renameBook', async () => {
  const b = await store.createBook({ premise: 'p', title: 'A' });
  await store.renameBook(b.id, 'B');
  assert.equal((await store.readBook(b.id)).title, 'B');
  await store.deleteBook(b.id);
  await assert.rejects(() => store.readBook(b.id), /BOOK_NOT_FOUND/);
});

test('versionSet + versionMove 往返（outline）', async () => {
  const b = await store.createBook({ premise: 'p' });
  await store.versionSet(b.id, 'outline', '第一版');
  await store.versionSet(b.id, 'outline', '第二版');
  let vf = await store.versionMove(b.id, 'outline', -1);
  assert.equal(store.currentText(vf), '第一版');
  const b2 = await store.readBook(b.id);
  assert.equal(store.currentText(b2.outline), '第一版');
});

test('versionSet 章节同步派生 content', async () => {
  const b = await store.createBook({ premise: 'p' });
  const s = await store.addSection(b.id, {});
  const c = await store.addChapter(b.id, s.id, {});
  const path = `section:${s.id}:chapter:${c.id}`;
  await store.versionSet(b.id, path, '章节正文A');
  const ch = await store.readChapter(b.id, s.id, c.id);
  assert.equal(store.currentText(ch.body), '章节正文A');
  assert.equal(ch.content, '章节正文A');
});
```

- [ ] **Step 2: 运行确认失败**

Run: `node --test server/test/store-versioned-io.test.js`
Expected: FAIL —— createBook 仍写老结构、迁移/书架/版本读写函数未实现。

- [ ] **Step 3: 改 `server/store.js`**

改 `emptyCore`（第 21-23 行）与 `emptyOutline`（第 20 行）：

```js
function emptyOutline() { return emptyVersioned(); }
function emptyCore() {
  return { core: {
    world: emptyVersioned(), style: emptyVersioned(),
    constraints: emptyVersioned(), pacing: emptyVersioned(),
  }, history: [] };
}
```

`addChapter`（第 99-112 行）里 chapter 对象改为：

```js
const chapter = {
  id, index, title: title || `第 ${index} 章`,
  body: emptyVersioned(), content: '',
  characters: [], summary: '', progress: '', status: 'done',
};
```

新增迁移辅助 + 改 readBook/readChapter/writeChapter：

```js
// ——— 惰性迁移辅助 ———
function migrateBookInPlace(book) {
  book.outline = migrateVersioned(book.outline);
  book.settings = book.settings || { core: {}, history: [] };
  const core = book.settings.core || {};
  for (const f of ['world', 'style', 'constraints', 'pacing']) core[f] = migrateVersioned(core[f]);
  book.settings.core = core;
  return book;
}
function migrateChapterInPlace(ch) {
  if (!ch.body || !Array.isArray(ch.body.versions)) {
    ch.body = migrateVersioned({ content: ch.content, history: ch.history });
  }
  ch.content = currentText(ch.body);  // 派生只读
  delete ch.history;                  // 老字段清理
  return ch;
}
```

`readBook`（第 40-47 行）末尾在 return 前迁移：

```js
export async function readBook(id) {
  try {
    const book = JSON.parse(await readFile(join(bookDir(id), 'book.json'), 'utf8'));
    return migrateBookInPlace(book);
  } catch (err) {
    if (err.code === 'ENOENT') throw new Error('BOOK_NOT_FOUND');
    throw err;
  }
}
```

`readChapter`（第 113-115 行）：

```js
export async function readChapter(bookId, sectionId, chapterId) {
  const ch = JSON.parse(await readFile(join(bookDir(bookId), safeId(sectionId), `${safeId(chapterId)}.json`), 'utf8'));
  return migrateChapterInPlace(ch);
}
```

`writeChapter`（第 116-118 行）落盘前同步派生 content：

```js
export async function writeChapter(bookId, sectionId, chapterId, obj) {
  if (obj.body) obj.content = currentText(obj.body);
  await atomicWriteJson(join(bookDir(bookId), safeId(sectionId), `${safeId(chapterId)}.json`), obj);
}
```

`listBooks`（第 54-70 行）加计数（在读到 book 后累加各 section 的 chapters 数）：

```js
export async function listBooks() {
  let ids = [];
  try { ids = await readdir(booksDir()); }
  catch (err) { if (err.code === 'ENOENT') return []; throw err; }
  const out = [];
  for (const id of ids) {
    try {
      const b = JSON.parse(await readFile(join(bookDir(id), 'book.json'), 'utf8'));
      let chapterCount = 0;
      for (const sid of b.sections || []) {
        try {
          const sec = JSON.parse(await readFile(join(bookDir(id), safeId(sid), 'section.json'), 'utf8'));
          chapterCount += (sec.chapters || []).length;
        } catch { /* 缺失的部跳过 */ }
      }
      out.push({ id: b.id, title: b.title, updatedAt: b.updatedAt, sectionCount: (b.sections || []).length, chapterCount });
    } catch (err) {
      if (err.code === 'ENOENT') continue;
      if (err.message === 'BAD_ID') continue;
      throw err;
    }
  }
  return out;
}
```

新增书架 + 版本读写（放文件末尾；需在顶部 import 补 `rm`）。把第 1 行 import 改为：
`import { mkdir, readFile, writeFile, rename, readdir, rm } from 'node:fs/promises';`

```js
// ——— 书架管理 ———
export async function deleteBook(id) {
  await rm(bookDir(id), { recursive: true, force: true });
}
export async function renameBook(id, title) {
  const book = await readBook(id);
  book.title = title || book.title;
  await writeBook(id, book);
  return book;
}

// ——— 统一版本读写 ———
export async function versionMove(bookId, path, delta) {
  const p = parseVersionPath(path);
  if (p.type === 'chapter') {
    const ch = await readChapter(bookId, p.sectionId, p.chapterId);
    moveCursor(ch.body, delta);
    await writeChapter(bookId, p.sectionId, p.chapterId, ch);
    return ch.body;
  }
  const b = await readBook(bookId);
  const vf = p.type === 'outline' ? b.outline : b.settings.core[p.field];
  moveCursor(vf, delta);
  await writeBook(bookId, b);
  return vf;
}
export async function versionSet(bookId, path, text) {
  const p = parseVersionPath(path);
  if (p.type === 'chapter') {
    const ch = await readChapter(bookId, p.sectionId, p.chapterId);
    commitVersion(ch.body, text);
    await writeChapter(bookId, p.sectionId, p.chapterId, ch);
    return ch.body;
  }
  const b = await readBook(bookId);
  const vf = p.type === 'outline' ? b.outline : b.settings.core[p.field];
  commitVersion(vf, text);
  await writeBook(bookId, b);
  return vf;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test server/test/store-versioned-io.test.js`
Expected: PASS。再跑 `node --test server/test/*.test.js` —— 注意：`store.test.js` 的 `createBook 建书并可读回` 断言 `assert.deepEqual(book.outline, { content: '', history: [] })` 会失败（结构变了）。同步修正该断言为 `assert.deepEqual(book.outline, { versions: [''], cursor: 0 });`。这是本任务对既有测试的必要适配，一并改。

- [ ] **Step 5: 提交**

```bash
git add server/store.js server/test/store-versioned-io.test.js server/test/store.test.js
git commit -m "feat(store): 惰性迁移+派生content+书架CRUD+统一版本读写"
```

---

### Task 3: prompts 适配版本化 core + 新增核心字段生成指令

**Files:**
- Modify: `server/prompts.js`
- Test: `server/test/prompts.test.js`（补充用例）

**Interfaces:**
- Consumes: 版本化 core（`buildSystemPrompt` 收到的 core 各字段现为 `{versions,cursor}`）。
- Produces（`server/prompts.js`）：
  - 内部纯辅助 `vtext(f)`：兼容 `{versions,cursor}` / `{content}` / 字符串 / 空。
  - `buildSystemPrompt(core)`：各字段走 `vtext`。
  - `buildContext(...)`：`book.outline` 走 `vtext`。
  - **新增** `buildCoreFieldInstruction(field, book): string`。

- [ ] **Step 1: 写失败测试（追加到 `server/test/prompts.test.js`）**

```js
import { buildCoreFieldInstruction, buildSystemPrompt } from '../prompts.js';
// ... 与文件既有 import 合并，勿重复

test('buildSystemPrompt 兼容版本化 core', () => {
  const s = buildSystemPrompt({
    world: { versions: ['赛博都市'], cursor: 0 },
    style: { versions: ['冷硬'], cursor: 0 },
    constraints: { versions: [''], cursor: 0 },
    pacing: { versions: [''], cursor: 0 },
  });
  assert.match(s, /赛博都市/);
  assert.match(s, /冷硬/);
});

test('buildCoreFieldInstruction 生成该字段指令', () => {
  const book = {
    premise: '一个侦探故事',
    settings: { core: {
      world: { versions: ['旧世界观'], cursor: 0 },
      style: { versions: ['冷硬'], cursor: 0 },
      constraints: { versions: [''], cursor: 0 },
      pacing: { versions: [''], cursor: 0 },
    } },
  };
  const ins = buildCoreFieldInstruction('world', book);
  assert.match(ins, /世界观/);
  assert.match(ins, /一个侦探故事/);
  assert.match(ins, /冷硬/);          // 带上其它字段作参照
});
```

（若 `prompts.test.js` 顶部已 `import assert from 'node:assert/strict'` 与 `test`，复用即可，不要重复声明。）

- [ ] **Step 2: 运行确认失败**

Run: `node --test server/test/prompts.test.js`
Expected: FAIL —— `buildCoreFieldInstruction` 未定义；`buildSystemPrompt` 拿到对象会渲染成 `[object Object]` 而非文本（断言 world 文本失败）。

- [ ] **Step 3: 改 `server/prompts.js`**

在文件顶部 `line` 之后加 `vtext`：

```js
// 读取可版本化字段的当前文本；兼容 {versions,cursor} / {content} / 字符串 / 空
function vtext(f) {
  if (!f) return '';
  if (Array.isArray(f.versions)) return f.versions[f.cursor] ?? '';
  if (typeof f.content === 'string') return f.content;
  return typeof f === 'string' ? f : '';
}
```

`buildSystemPrompt`（第 3-11 行）改为走 vtext：

```js
export function buildSystemPrompt(core = {}) {
  let s = '你是一位小说写手。严格遵守以下设定：\n';
  s += line('世界观', vtext(core.world));
  s += line('文风基调', vtext(core.style));
  s += line('禁忌约束', vtext(core.constraints));   // 硬约束，最高优先级
  s += line('篇幅节奏', vtext(core.pacing));
  return s.trim();
}
```

`buildContext`（第 13 行起）里 `line('全书大纲', book.outline?.content)` 改为 `line('全书大纲', vtext(book.outline))`。其余不变（section.outline 仍 `{content}`，`prevChapter.content` 是派生字符串，均由 vtext/原样兼容）。

文件末尾新增：

```js
export function buildCoreFieldInstruction(field, book) {
  const names = { world: '世界观', style: '文风基调', constraints: '禁忌约束', pacing: '篇幅节奏' };
  const core = (book.settings && book.settings.core) || {};
  let others = '';
  for (const k of Object.keys(names)) {
    if (k === field) continue;
    others += line(names[k], vtext(core[k]));
  }
  return `这本书的故事设想：『${book.premise || ''}』。\n已有设定：\n${others}` +
    `请为这本书重新拟定『${names[field]}』，200 字内，与其它设定保持一致，只输出该项内容，不要解释、不要标题。`;
}
```

- [ ] **Step 4: 运行确认通过**

Run: `node --test server/test/prompts.test.js`
Expected: PASS。再跑全量确认无回归。

- [ ] **Step 5: 提交**

```bash
git add server/prompts.js server/test/prompts.test.js
git commit -m "feat(prompts): 兼容版本化 core + 新增核心字段生成指令"
```

---

### Task 4: books.js 路由（version/move·clear·save + 书架 DELETE/PATCH + 删旧端点）

**Files:**
- Modify: `server/routes/books.js`
- Test: `server/test/books.test.js`（改写旧端点用例）

**Interfaces:**
- Consumes: Task 2 的 `versionMove/versionSet/deleteBook/renameBook`。
- Produces（HTTP 端点）：
  - `POST /api/books/:id/version/move  { path, delta }` → `{versions,cursor}`
  - `POST /api/books/:id/version/clear { path }` → `{versions,cursor}`（= versionSet '' ）
  - `POST /api/books/:id/version/save  { path, text }` → `{versions,cursor}`
  - `DELETE /api/books/:id` → `{ok:true}`
  - `PATCH  /api/books/:id { title }` → 更新后 book
  - **移除**：`PUT /outline`、`PUT /core`、`PUT .../chapters/:cid`、`POST .../rollback`

- [ ] **Step 1: 改 `server/routes/books.js`**

删除第 34-64 行（outline PUT / core PUT / chapter PUT / rollback POST 四个），替换为：

```js
  // ——— 统一版本操作 ———
  app.post('/api/books/:id/version/move', async (req, res) => {
    try {
      const vf = await store.versionMove(req.params.id, req.body?.path, Number(req.body?.delta) || 0);
      res.json(vf);
    } catch (e) { res.status(400).json({ error: String(e.message || e) }); }
  });
  app.post('/api/books/:id/version/clear', async (req, res) => {
    try { res.json(await store.versionSet(req.params.id, req.body?.path, '')); }
    catch (e) { res.status(400).json({ error: String(e.message || e) }); }
  });
  app.post('/api/books/:id/version/save', async (req, res) => {
    try { res.json(await store.versionSet(req.params.id, req.body?.path, req.body?.text ?? '')); }
    catch (e) { res.status(400).json({ error: String(e.message || e) }); }
  });

  // ——— 书架管理 ———
  app.delete('/api/books/:id', async (req, res) => {
    try { await store.deleteBook(req.params.id); res.json({ ok: true }); }
    catch (e) { res.status(400).json({ error: String(e.message || e) }); }
  });
  app.patch('/api/books/:id', async (req, res) => {
    try { res.json(await store.renameBook(req.params.id, req.body?.title)); }
    catch (e) { res.status(400).json({ error: String(e.message || e) }); }
  });
```

- [ ] **Step 2: 改 `server/test/books.test.js`**

`手动编辑正文入 history，回退可还原`（第 32-46 行）改写为版本 save + move：

```js
test('版本 save 入链，move 双向浏览', async () => {
  await withServer(async () => {
    const book = await j(await post('/api/books', { premise: 'p' }));
    const s = await j(await post(`/api/books/${book.id}/sections`, {}));
    const c = await j(await post(`/api/books/${book.id}/sections/${s.id}/chapters`, {}));
    const path = `section:${s.id}:chapter:${c.id}`;
    await post(`/api/books/${book.id}/version/save`, { path, text: '第一版' });
    let vf = await j(await post(`/api/books/${book.id}/version/save`, { path, text: '第二版' }));
    assert.equal(vf.versions[vf.cursor], '第二版');
    vf = await j(await post(`/api/books/${book.id}/version/move`, { path, delta: -1 }));
    assert.equal(vf.versions[vf.cursor], '第一版');       // 回退
    vf = await j(await post(`/api/books/${book.id}/version/move`, { path, delta: 1 }));
    assert.equal(vf.versions[vf.cursor], '第二版');       // 再前进
  });
});

test('version/clear 清空为新版，可 move 回', async () => {
  await withServer(async () => {
    const book = await j(await post('/api/books', { premise: 'p' }));
    const path = 'outline';
    await post(`/api/books/${book.id}/version/save`, { path, text: '有内容' });
    let vf = await j(await post(`/api/books/${book.id}/version/clear`, { path }));
    assert.equal(vf.versions[vf.cursor], '');
    vf = await j(await post(`/api/books/${book.id}/version/move`, { path, delta: -1 }));
    assert.equal(vf.versions[vf.cursor], '有内容');
  });
});

test('非法 path 返回 400', async () => {
  await withServer(async () => {
    const book = await j(await post('/api/books', { premise: 'p' }));
    const r = await post(`/api/books/${book.id}/version/move`, { path: 'core:evil', delta: 1 });
    assert.equal(r.status, 400);
  });
});

test('DELETE 删书 / PATCH 改名', async () => {
  await withServer(async () => {
    const book = await j(await post('/api/books', { premise: 'p', title: 'A' }));
    const patched = await j(await fetch(`${base}/api/books/${book.id}`, {
      method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title: 'B' }),
    }));
    assert.equal(patched.title, 'B');
    const del = await j(await fetch(`${base}/api/books/${book.id}`, { method: 'DELETE' }));
    assert.equal(del.ok, true);
    const list = await j(await fetch(`${base}/api/books`));
    assert.equal(list.find((x) => x.id === book.id), undefined);
  });
});
```

（`put` 辅助若不再被使用，删除其声明以免 lint 噪声；`post`/`j` 保留。）

- [ ] **Step 3: 运行确认通过**

Run: `node --test server/test/books.test.js`
Expected: PASS。再跑全量。

- [ ] **Step 4: 提交**

```bash
git add server/routes/books.js server/test/books.test.js
git commit -m "feat(routes): 统一版本操作端点 + 书架 DELETE/PATCH，删除旧端点"
```

---

### Task 5: gen.js（章节写 body 走版本化；新增 version/rewrite 服务 outline+core；移除 gen/outline）

**Files:**
- Modify: `server/routes/gen.js`
- Test: `server/test/gen.test.js`（适配）

**Interfaces:**
- Consumes: Task 2 `commitVersion/versionSet`、Task 3 `buildCoreFieldInstruction`；注入的 `streamChat/nonStreamChat/extractDigest`。
- Produces（HTTP 端点，均 SSE）：
  - `POST /api/gen/chapter { bookId, sectionId, chapterId?, mode:'next'|'rewrite'|'whip', whip? }`：正文写入 `chapter.body`（`commitVersion`），保留 digest 冒泡与空章回滚。
  - `POST /api/books/:id/version/rewrite { path }`：仅服务 `outline` 与 `core:*`；流式生成后 `versionSet` 落盘（内部 commit）。`done` 带 `{versions,cursor}`。章节 path 由 `/api/gen/chapter` 承担（本端点收到章节 path 返回 error）。
  - **移除** `POST /api/gen/outline`（大纲改由 version/rewrite 承担）。
- **决策（记录在案）**：spec §3.2 设想 version/rewrite 也覆盖章节；本实现让章节沿用已成熟的 `/api/gen/chapter`（含上下文构造 + digest + 空章回滚），version/rewrite 只做 outline/core 的简单重生——避免复制章节生成管线，符合去重原则。前端据 path 类型分派（Task 9）。

- [ ] **Step 1: 改 `server/routes/gen.js`**

删除 `app.post('/api/gen/outline', ...)`（第 29-42 行）。

`gen/chapter`（第 60-132 行）里两处正文写入改为版本化：
- 把第 79 行 `store.pushHistory(chapter, 'content');` 删除（rewrite/whip 不再用 pushHistory）。
- 把第 94-95 行
  ```js
  chapter.content = full;
  await store.writeChapter(bookId, sectionId, chapterId, chapter);
  ```
  改为
  ```js
  store.commitVersion(chapter.body, full);      // 写入新版；writeChapter 会同步派生 content
  await store.writeChapter(bookId, sectionId, chapterId, chapter);
  ```
- digest 段落不变（读的是 `full` 与 `chapter.summary/progress/characters`）。空章回滚逻辑不变。

在 `mountGenRoutes` 内新增 version/rewrite（复用 `streamInto`）：

```js
  app.post('/api/books/:id/version/rewrite', async (req, res) => {
    sseInit(res);
    try {
      const { path } = req.body || {};
      const p = store.parseVersionPath(path);       // 非法 path 抛 BAD_PATH
      if (p.type === 'chapter') throw new Error('章节请用 /api/gen/chapter');
      const config = await store.readConfig();
      const book = await store.readBook(req.params.id);
      const system = buildSystemPrompt(book.settings.core);
      const instruction = p.type === 'outline'
        ? buildOutlineInstruction(book.premise)
        : buildCoreFieldInstruction(p.field, book);
      const full = await streamInto(res, { config, system, instruction });
      const vf = await store.versionSet(req.params.id, path, full);
      send(res, { done: true, versions: vf.versions, cursor: vf.cursor });
    } catch (e) { send(res, { error: String(e.message || e) }); }
    res.end();
  });
```

顶部 import（第 2-5 行）补 `buildCoreFieldInstruction`：
`import { buildSystemPrompt, buildContext, buildChapterInstruction, buildOutlineInstruction, buildSectionsInstruction, buildCoreFieldInstruction, DIGEST_INSTRUCTION } from '../prompts.js';`

- [ ] **Step 2: 适配 `server/test/gen.test.js`**

先读该文件了解现有 stub 结构（它注入假的 streamChat）。改动点：
- 若有针对 `/api/gen/outline` 的用例 → 改为打 `/api/books/:id/version/rewrite { path:'outline' }`，断言 `done` 后 `readBook(id)` 的 `currentText(book.outline)` 等于假流文本。
- 针对 `/api/gen/chapter` 的用例：断言正文进入 `chapter.body`（`store.currentText(ch.body)` 等于流文本），而非 `ch.content`（content 仍等值，因派生）。
- 新增一例：`version/rewrite { path:'core:world' }` 后 `readBook` 的 `currentText(book.settings.core.world)` 等于流文本。

（具体断言按该文件既有 stub 风格写；保持假 LLM 注入不变。）

- [ ] **Step 3: 运行确认通过**

Run: `node --test server/test/gen.test.js` 然后 `node --test server/test/*.test.js`
Expected: 全 PASS。

- [ ] **Step 4: 提交**

```bash
git add server/routes/gen.js server/test/gen.test.js
git commit -m "feat(gen): 章节正文版本化 + version/rewrite 服务大纲与核心字段，移除 gen/outline"
```

---

### Task 6: 前端类型 + versioned 纯函数 + api 客户端

**Files:**
- Modify: `web/src/types.ts`
- Create: `web/src/versioned.ts`
- Create: `web/src/versioned.test.ts`
- Modify: `web/src/api.ts`
- Modify: `web/src/store.ts`（findChapter/firstSelectable 适配——签名不变，仅确保读 body 无碍）

**Interfaces:**
- Produces:
  - `web/src/types.ts`：`Versioned{versions:string[];cursor:number}`；`CoreSettings` 四字段为 `Versioned`；`Chapter` 加 `body:Versioned`、`content:string`（派生）、去 `history`；`Book.outline:Versioned`；新增 `BookSummary{id;title;updatedAt;sectionCount;chapterCount}`。
  - `web/src/versioned.ts`：`currentText/canPrev/canNext/versionLabel/bookSpineColor`（纯）。
  - `web/src/api.ts`：`listBooks():Promise<BookSummary[]>`、`deleteBook(id)`、`renameBook(id,title)`、`versionMove(bookId,path,delta)`、`versionClear(bookId,path)`、`versionSave(bookId,path,text)`、`versionRewritePath(bookId)`（返回 rewrite URL 供 streamGen 用）；移除 `saveChapter/rollbackChapter/saveOutline/saveCore`。
- Consumes（后端契约）：Task 2/4/5 端点。

- [ ] **Step 1: 写失败测试 `web/src/versioned.test.ts`**

```ts
import { describe, it, expect } from 'vitest';
import { currentText, canPrev, canNext, versionLabel, bookSpineColor } from './versioned';

describe('versioned 纯函数', () => {
  const v = { versions: ['a', 'b', 'c'], cursor: 1 };
  it('currentText 取当前版', () => expect(currentText(v)).toBe('b'));
  it('canPrev/canNext 边界', () => {
    expect(canPrev(v)).toBe(true); expect(canNext(v)).toBe(true);
    expect(canPrev({ versions: ['x'], cursor: 0 })).toBe(false);
    expect(canNext({ versions: ['x'], cursor: 0 })).toBe(false);
  });
  it('versionLabel', () => expect(versionLabel(v)).toBe('第 2 / 3 版'));
  it('bookSpineColor 稳定且合法 hsl', () => {
    expect(bookSpineColor('book_1')).toBe(bookSpineColor('book_1'));
    expect(bookSpineColor('book_1')).toMatch(/^hsl\(\d{1,3}, \d+%, \d+%\)$/);
  });
});
```

- [ ] **Step 2: 运行确认失败**

Run（cwd=web）: `npm test`
Expected: FAIL —— 找不到 `./versioned`。

- [ ] **Step 3: 写 `web/src/versioned.ts`**

```ts
import type { Versioned } from './types';

export const currentText = (v: Versioned): string => v.versions[v.cursor] ?? '';
export const canPrev = (v: Versioned): boolean => v.cursor > 0;
export const canNext = (v: Versioned): boolean => v.cursor < v.versions.length - 1;
export const versionLabel = (v: Versioned): string => `第 ${v.cursor + 1} / ${v.versions.length} 版`;

// 按书 id 生成稳定色相，纯 CSS 书脊色（无图片请求）
export function bookSpineColor(id: string): string {
  let h = 0;
  for (let i = 0; i < id.length; i++) h = (h * 31 + id.charCodeAt(i)) % 360;
  return `hsl(${h}, 45%, 60%)`;
}
```

- [ ] **Step 4: 改 `web/src/types.ts`**

```ts
export interface Character { name: string; role: string; desc: string; }
export interface Versioned { versions: string[]; cursor: number; }
export interface Outline { content: string; history: string[]; }   // 仅 section.outline 仍用
export interface CoreSettings { world: Versioned; style: Versioned; constraints: Versioned; pacing: Versioned; }
export interface Chapter {
  id: string; index: number; title: string;
  body: Versioned; content: string;                                  // content 为派生只读
  characters: Character[]; summary: string; progress: string; status: string;
}
export interface Section {
  id: string; index: number; title: string; outline: Outline;
  characters: Character[]; summary: string; progress: string;
  chapters: string[] | Chapter[];
}
export interface Book {
  id: string; title: string; createdAt: string; updatedAt: string;
  premise: string; outline: Versioned;
  settings: { core: CoreSettings; history: string[] };
  characters: Character[]; summary: string; progress: string; sections: string[];
}
export interface BookTree { book: Book; sections: (Section & { chapters: Chapter[] })[]; }
export interface BookSummary { id: string; title: string; updatedAt: string; sectionCount: number; chapterCount: number; }
export interface Config { baseUrl: string; model: string; apiKey: string; chapterWordTarget: number; }
```

- [ ] **Step 5: 改 `web/src/api.ts`**

替换第 11-21 行区域（books/version 相关）为：

```ts
export const listBooks = (): Promise<import('./types').BookSummary[]> => fetch('/api/books').then(json);
export const createBook = (premise: string, title?: string): Promise<Book> => jpost('/api/books', { premise, title });
export const getTree = (bookId: string): Promise<BookTree> => fetch(`/api/books/${bookId}/tree`).then(json);
export const addSection = (bookId: string, title?: string) => jpost(`/api/books/${bookId}/sections`, { title });
export const addChapter = (bookId: string, sid: string, title?: string) => jpost(`/api/books/${bookId}/sections/${sid}/chapters`, { title });

// 书架管理
export const deleteBook = (bookId: string) => fetch(`/api/books/${bookId}`, { method: 'DELETE' }).then(json);
export const renameBook = (bookId: string, title: string) =>
  fetch(`/api/books/${bookId}`, { method: 'PATCH', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ title }) }).then(json);

// 统一版本操作
export const versionMove = (bookId: string, path: string, delta: number) => jpost(`/api/books/${bookId}/version/move`, { path, delta });
export const versionClear = (bookId: string, path: string) => jpost(`/api/books/${bookId}/version/clear`, { path });
export const versionSave = (bookId: string, path: string, text: string) => jpost(`/api/books/${bookId}/version/save`, { path, text });
export const rewriteUrl = (bookId: string) => `/api/books/${bookId}/version/rewrite`;
```

（`BookTree`、`Book` 已在顶部 import；`BookSummary` 用内联 import 或加到顶部 import 列表——择一，保持 tsc 通过。移除旧的 `saveChapter/rollbackChapter/saveOutline/saveCore` 定义。`streamGen`/`getConfig`/`saveConfig`/`SSEEvent`/`parseSSELines` 保留不变。）

- [ ] **Step 6: 改 `web/src/store.ts`**

`findChapter`/`firstSelectable` 逻辑不变（不读 content/body），确认类型仍编译通过即可。若 `store.test.ts` 用 `as unknown as BookTree` 构造，则不受结构变更影响，无需改。

- [ ] **Step 7: 运行测试 + 类型检查**

Run（cwd=web）: `npm test`
Expected: `versioned` 用例 PASS；`store/api/theme/toast/sections` 仍 PASS。
Run: `npm run build`
Expected: **会红**——`MainPanel.tsx`/`App.tsx`/`Actions.tsx` 仍引用旧结构（`chapter.content` 只读渲染、`saveChapter` 等已删）。这是预期的临时不一致，Task 7-9 收口。本任务只需确认新文件（versioned.ts）与 types.ts/api.ts 本身类型自洽，错误只出现在尚未改造的组件里。

- [ ] **Step 8: 提交**

```bash
git add web/src/types.ts web/src/versioned.ts web/src/versioned.test.ts web/src/api.ts web/src/store.ts
git commit -m "feat(web): 版本化类型 + versioned 纯函数 + 版本/书架 API 客户端"
```

---

### Task 7: VersionedBox 通用组件

**Files:**
- Create: `web/src/components/VersionedBox.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `Versioned`（types）、`currentText/canPrev/canNext/versionLabel`（versioned）、`.sketch/.hbtn/.cursor`（styles）。
- Produces（`web/src/components/VersionedBox.tsx`）：
  - `export function VersionedBox(props)`，props：
    - `title: string`
    - `versioned: Versioned`
    - `streaming: boolean`
    - `streamingText: string`
    - `onMove: (delta: number) => void`
    - `onRewrite: () => void`
    - `onClear: () => void`
    - `onSave: (text: string) => void`
    - `onStop: () => void`

- [ ] **Step 1: 写 `web/src/components/VersionedBox.tsx`**

（纯视觉 + 交互组件，无独立单测；正确性由 tsc + 手动验收保证。删除用内联二次确认，不用系统 alert。）

```tsx
import { useEffect, useState } from 'react';
import type { Versioned } from '../types';
import { currentText, canPrev, canNext, versionLabel } from '../versioned';

export function VersionedBox({ title, versioned, streaming, streamingText, onMove, onRewrite, onClear, onSave, onStop }: {
  title: string;
  versioned: Versioned;
  streaming: boolean;
  streamingText: string;
  onMove: (delta: number) => void;
  onRewrite: () => void;
  onClear: () => void;
  onSave: (text: string) => void;
  onStop: () => void;
}) {
  const cur = currentText(versioned);
  const [draft, setDraft] = useState(cur);
  const [confirmClear, setConfirmClear] = useState(false);
  useEffect(() => { setDraft(cur); }, [cur]);

  return (
    <section className="vbox sketch">
      <div className="vbox-head">
        <h3 className="vbox-title">{title}</h3>
        <div className="vbox-tools">
          <button className="hbtn mini" disabled={streaming || !canPrev(versioned)} onClick={() => onMove(-1)}>◀ 上一个</button>
          {streaming
            ? <button className="hbtn mini stop" onClick={onStop}>⏹ 停止</button>
            : <button className="hbtn mini" onClick={onRewrite}>🔄 重写</button>}
          <button className="hbtn mini" disabled={streaming || !canNext(versioned)} onClick={() => onMove(1)}>下一个 ▶</button>
          {confirmClear
            ? <button className="hbtn mini accent" onClick={() => { setConfirmClear(false); onClear(); }}>确认清空？</button>
            : <button className="hbtn mini" disabled={streaming} onClick={() => setConfirmClear(true)}>🗑 删除</button>}
        </div>
      </div>
      {streaming
        ? <pre className="vbox-body">{streamingText}<span className="cursor">▎</span></pre>
        : <textarea className="vbox-body editor" value={draft}
            onChange={(e) => setDraft(e.target.value)}
            onBlur={() => { if (draft !== cur) onSave(draft); }} />}
      <div className="vbox-foot">{versionLabel(versioned)}</div>
    </section>
  );
}
```

- [ ] **Step 2: 追加样式到 `web/src/styles.css`**

```css
/* ===== VersionedBox 版本化框 ===== */
.vbox { padding: 16px 18px; margin: 0 0 16px; display: flex; flex-direction: column; gap: 10px; }
.vbox-head { display: flex; align-items: center; gap: 12px; flex-wrap: wrap; }
.vbox-title { margin: 0; font-size: 20px; border-bottom: 2px solid var(--accent); padding-bottom: 2px; }
.vbox-tools { margin-left: auto; display: flex; gap: 6px; flex-wrap: wrap; }
.vbox-body { width: 100%; min-height: 180px; font-size: 16px; line-height: 1.8; }
.vbox-body.editor { border: none; outline: none; background: transparent; resize: vertical; }
.vbox-body.editor:focus { outline: 2px dashed var(--line); outline-offset: 6px; border-radius: 8px; }
.vbox-foot { color: var(--ink-soft); font-size: 13px; text-align: right; }
```

- [ ] **Step 3: 类型检查（隔离）**

Run（cwd=web）: `npm run build`
Expected: 仍红，但错误只应来自 `MainPanel.tsx`/`App.tsx`/`Actions.tsx`（未改造）。通读确认 `VersionedBox.tsx` 自身无类型错误、所有 import 被使用。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/VersionedBox.tsx web/src/styles.css
git commit -m "feat(web): VersionedBox 通用版本化框（行内工具条+双向浏览+内联删除确认）"
```

---

### Task 8: Bookshelf 书架组件

**Files:**
- Create: `web/src/components/Bookshelf.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: `BookSummary`（types）、`bookSpineColor`（versioned）、`.sketch/.hbtn`（styles）。
- Produces（`web/src/components/Bookshelf.tsx`）：
  - `export function Bookshelf(props)`，props：
    - `books: BookSummary[]`
    - `onOpen: (id: string) => void`
    - `onNew: () => void`
    - `onRename: (id: string, title: string) => void`
    - `onDelete: (id: string) => void`

- [ ] **Step 1: 写 `web/src/components/Bookshelf.tsx`**

```tsx
import { useState } from 'react';
import type { BookSummary } from '../types';
import { bookSpineColor } from '../versioned';

export function Bookshelf({ books, onOpen, onNew, onRename, onDelete }: {
  books: BookSummary[];
  onOpen: (id: string) => void;
  onNew: () => void;
  onRename: (id: string, title: string) => void;
  onDelete: (id: string) => void;
}) {
  const [renaming, setRenaming] = useState<string | null>(null);
  const [draft, setDraft] = useState('');
  const [confirmDel, setConfirmDel] = useState<string | null>(null);

  return (
    <div className="shelf">
      <h1 className="shelf-title">📚 我的书架</h1>
      <div className="shelf-grid">
        <button className="shelf-card new sketch" onClick={onNew}>＋ 新建一本</button>
        {books.map((b) => (
          <div key={b.id} className="shelf-card sketch">
            <div className="spine" style={{ background: bookSpineColor(b.id) }} />
            {renaming === b.id ? (
              <input className="shelf-rename" autoFocus value={draft}
                onChange={(e) => setDraft(e.target.value)}
                onKeyDown={(e) => { if (e.key === 'Enter' && draft.trim()) { onRename(b.id, draft.trim()); setRenaming(null); } }} />
            ) : (
              <div className="shelf-name" onClick={() => onOpen(b.id)}>{b.title || '未命名'}</div>
            )}
            <div className="shelf-meta">{b.sectionCount} 部 · {b.chapterCount} 章</div>
            <div className="shelf-meta dim">{new Date(b.updatedAt).toLocaleString()}</div>
            <div className="shelf-ops">
              {renaming === b.id
                ? <button className="hbtn mini" onClick={() => { if (draft.trim()) { onRename(b.id, draft.trim()); } setRenaming(null); }}>保存</button>
                : <button className="hbtn mini" onClick={() => { setRenaming(b.id); setDraft(b.title); }}>✏️ 改名</button>}
              {confirmDel === b.id
                ? <button className="hbtn mini accent" onClick={() => { setConfirmDel(null); onDelete(b.id); }}>确认删除？</button>
                : <button className="hbtn mini" onClick={() => setConfirmDel(b.id)}>🗑 删除</button>}
            </div>
          </div>
        ))}
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 追加样式到 `web/src/styles.css`**

```css
/* ===== 书架 ===== */
.shelf { max-width: 960px; margin: 0 auto; padding: 32px 20px; }
.shelf-title { font-size: 28px; margin: 0 0 20px; }
.shelf-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); gap: 18px; }
.shelf-card { position: relative; padding: 16px 16px 16px 24px; min-height: 150px; display: flex; flex-direction: column; gap: 6px; background: var(--panel); }
.shelf-card.new { align-items: center; justify-content: center; font-size: 18px; color: var(--ink-soft); border-style: dashed; cursor: pointer; }
.spine { position: absolute; left: 0; top: 0; bottom: 0; width: 8px; border-radius: 6px 0 0 6px; }
.shelf-name { font-size: 19px; font-weight: 700; cursor: pointer; }
.shelf-name:hover { color: var(--accent); }
.shelf-rename { font-size: 18px; padding: 4px 6px; border: 2px solid var(--line); border-radius: 8px; background: var(--bg); }
.shelf-meta { color: var(--ink-soft); font-size: 13px; }
.shelf-meta.dim { opacity: .7; }
.shelf-ops { margin-top: auto; display: flex; gap: 6px; }
```

- [ ] **Step 3: 类型检查（隔离）**

Run（cwd=web）: `npm run build`
Expected: 仍红（限 MainPanel/App/Actions）。通读确认 `Bookshelf.tsx` 自身类型正确、import 全用到。

- [ ] **Step 4: 提交**

```bash
git add web/src/components/Bookshelf.tsx web/src/styles.css
git commit -m "feat(web): Bookshelf 书架（卡片概览+改名+删除+新建+稳定色书脊）"
```

---

### Task 9: MainPanel/Actions 重构 + App 集成（收口至全绿）

**Files:**
- Rewrite: `web/src/components/MainPanel.tsx`
- Rewrite: `web/src/components/Actions.tsx`
- Rewrite: `web/src/App.tsx`
- Append: `web/src/styles.css`

**Interfaces:**
- Consumes: Task 6 类型/api、Task 7 `VersionedBox`、Task 8 `Bookshelf`、既有 `TopBar/Toast/Sidebar/SettingsPage/FirstRun/SectionPlanPanel`。
- Produces（收口）：`tsc -b` 零错误、`npm run build` 成功、`npm test` 全绿。

**streaming 模型**：App 持 `streaming:boolean`、`streamingText:string`、`streamingPath: string | null`（取值 `'outline' | 'core:world|style|constraints|pacing' | 'chapter' | null`）。各框据 `streamingPath` 判断自己是否在流式。章节任意生成（next/whip/rewrite）统一用 `'chapter'` 哨兵。

- [ ] **Step 1: 重写 `web/src/components/Actions.tsx`**（收窄为章节专属「下一章 / 抽打」，重写/上一版移入框工具条）

```tsx
import { useState } from 'react';

export function Actions({ streaming, onNext, onWhip, onStop }: {
  streaming: boolean;
  onNext: () => void;
  onWhip: (text: string) => void;
  onStop: () => void;
}) {
  const [whip, setWhip] = useState('');
  return (
    <div className="actions sketch">
      {streaming ? (
        <button className="hbtn stop" onClick={onStop}>⏹ 停止</button>
      ) : (
        <>
          <div className="btn-row">
            <button className="hbtn accent-2" onClick={onNext}>➡️ 下一章</button>
          </div>
          <div className="whip-row">
            <textarea className="whip-input" placeholder="狠狠抽打：写下你的不满与要求…" value={whip}
              onChange={(e) => setWhip(e.target.value)} />
            <button className="hbtn accent whip-btn" onClick={() => { if (whip.trim()) { onWhip(whip); setWhip(''); } }}>🗯️ 抽</button>
          </div>
        </>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 重写 `web/src/components/MainPanel.tsx`**（三视图改用 VersionedBox；path 与 streaming 分派）

```tsx
import type { BookTree } from '../types';
import type { Selection } from '../store';
import { findChapter } from '../store';
import { VersionedBox } from './VersionedBox';

const CORE_FIELDS: { field: 'world' | 'style' | 'constraints' | 'pacing'; label: string }[] = [
  { field: 'world', label: '世界观' }, { field: 'style', label: '文风基调' },
  { field: 'constraints', label: '禁忌约束' }, { field: 'pacing', label: '篇幅节奏' },
];

export function MainPanel({ tree, selection, streaming, streamingText, streamingPath, onMove, onRewrite, onClear, onSave, onStop }: {
  tree: BookTree; selection: Selection;
  streaming: boolean; streamingText: string; streamingPath: string | null;
  onMove: (path: string, delta: number) => void;
  onRewrite: (path: string) => void;
  onClear: (path: string) => void;
  onSave: (path: string, text: string) => void;
  onStop: () => void;
}) {
  const boxProps = (path: string) => ({
    streaming: streaming && streamingPath === path,
    streamingText: streaming && streamingPath === path ? streamingText : '',
    onMove: (d: number) => onMove(path, d),
    onRewrite: () => onRewrite(path),
    onClear: () => onClear(path),
    onSave: (t: string) => onSave(path, t),
    onStop,
  });

  if (selection.kind === 'outline') {
    return <main className="main"><VersionedBox title="全书大纲" versioned={tree.book.outline} {...boxProps('outline')} /></main>;
  }

  if (selection.kind === 'core') {
    return (
      <main className="main">
        {CORE_FIELDS.map(({ field, label }) => (
          <VersionedBox key={field} title={label} versioned={tree.book.settings.core[field]} {...boxProps(`core:${field}`)} />
        ))}
      </main>
    );
  }

  const chapter = findChapter(tree, selection);
  if (!chapter) {
    return <main className="main"><div className="empty-hint big">还没有章节。点左侧 <b>＋ 新建部</b> 或 <b>🧩 AI 规划分部</b> 开始，再 <b>＋ 加章</b>。</div></main>;
  }
  const path = `section:${selection.sectionId}:chapter:${selection.chapterId}`;
  // 章节任意生成用 'chapter' 哨兵
  const chStreaming = streaming && streamingPath === 'chapter';
  return (
    <main className="main">
      <VersionedBox title={chapter.title} versioned={chapter.body}
        streaming={chStreaming} streamingText={chStreaming ? streamingText : ''}
        onMove={(d) => onMove(path, d)} onRewrite={() => onRewrite(path)}
        onClear={() => onClear(path)} onSave={(t) => onSave(path, t)} onStop={onStop} />
    </main>
  );
}
```

- [ ] **Step 3: 重写 `web/src/App.tsx`**（书架落点 + 版本操作分派 + 书架 CRUD + 收口）

```tsx
import { useEffect, useRef, useState } from 'react';
import type { Book, BookTree, BookSummary } from './types';
import type { Selection } from './store';
import { firstSelectable } from './store';
import * as api from './api';
import { useToast } from './components/Toast';
import { TopBar } from './components/TopBar';
import { Sidebar } from './components/Sidebar';
import { MainPanel } from './components/MainPanel';
import { Actions } from './components/Actions';
import { SettingsPage } from './components/SettingsPage';
import { FirstRun } from './components/FirstRun';
import { SectionPlanPanel } from './components/SectionPlanPanel';
import { Bookshelf } from './components/Bookshelf';

type View = 'shelf' | 'book';

export default function App() {
  const toast = useToast();
  const [loading, setLoading] = useState(true);
  const [view, setView] = useState<View>('shelf');
  const [books, setBooks] = useState<BookSummary[]>([]);
  const [creating, setCreating] = useState(false);
  const [tree, setTree] = useState<BookTree | null>(null);
  const [selection, setSelection] = useState<Selection>({ kind: 'outline' });
  const [streaming, setStreaming] = useState(false);
  const [streamingText, setStreamingText] = useState('');
  const [streamingPath, setStreamingPath] = useState<string | null>(null);
  const [statusText, setStatusText] = useState('');
  const [showSettings, setShowSettings] = useState(false);
  const [planOpen, setPlanOpen] = useState(false);
  const [planText, setPlanText] = useState('');
  const abortRef = useRef<null | (() => void)>(null);

  const loadShelf = async () => { const list = await api.listBooks(); setBooks(list); return list; };
  useEffect(() => { loadShelf().finally(() => setLoading(false)); }, []);

  const openBook = async (id: string) => {
    const t = await api.getTree(id); setTree(t); setSelection(firstSelectable(t)); setView('book');
  };
  const reload = async (bookId: string, sel?: Selection) => {
    const t = await api.getTree(bookId); setTree(t); if (sel) setSelection(sel);
  };
  const goShelf = async () => { setView('shelf'); await loadShelf(); };

  if (showSettings) return <SettingsPage onClose={() => setShowSettings(false)} />;
  if (loading) return <div className="boot-skeleton"><div className="sk-line" /><div className="sk-line" /><div className="sk-line short" /></div>;

  // 书架视图
  if (view === 'shelf') {
    if (creating || books.length === 0) {
      return <FirstRun onCreated={async (b: Book) => { setCreating(false); await openBook(b.id); }} />;
    }
    return <Bookshelf books={books}
      onOpen={openBook}
      onNew={() => setCreating(true)}
      onRename={async (id, title) => { await api.renameBook(id, title); await loadShelf(); toast.success('✓ 已改名'); }}
      onDelete={async (id) => { await api.deleteBook(id); await loadShelf(); toast.success('✓ 已删除'); }} />;
  }

  if (!tree) return <div className="boot-skeleton"><div className="sk-line" /></div>;
  const bookId = tree.book.id;

  // —— 版本操作 ——
  const doMove = async (path: string, delta: number) => { await api.versionMove(bookId, path, delta); await reload(bookId); };
  const doClear = async (path: string) => { await api.versionClear(bookId, path); await reload(bookId); toast.info('已清空（可用「上一个」找回）'); };
  const doSave = async (path: string, text: string) => { await api.versionSave(bookId, path, text); await reload(bookId); toast.success('✓ 已保存'); };

  const stageFor = (path: string) =>
    path === 'outline' ? '✍️ 正在重写全书大纲…' : path.startsWith('core:') ? '✍️ 正在重写设定…' : '✍️ 正在重写本章…';

  const doRewrite = (path: string) => {
    const isChapter = path.startsWith('section:');
    setStreaming(true); setStreamingText(''); setStatusText(stageFor(path));
    if (isChapter) {
      setStreamingPath('chapter');
      const [, sectionId, , chapterId] = path.split(':');
      abortRef.current = api.streamGen('/api/gen/chapter', { bookId, sectionId, chapterId, mode: 'rewrite' }, {
        onDelta: (d) => setStreamingText((t) => t + d),
        onError: (m) => { setStreaming(false); setStreamingPath(null); setStatusText(''); toast.error('生成失败：' + m); },
        onDone: async () => { setStreaming(false); setStreamingPath(null); setStatusText(''); await reload(bookId, selection); toast.success('✓ 已重写'); },
      });
    } else {
      setStreamingPath(path);
      abortRef.current = api.streamGen(api.rewriteUrl(bookId), { path }, {
        onDelta: (d) => setStreamingText((t) => t + d),
        onError: (m) => { setStreaming(false); setStreamingPath(null); setStatusText(''); toast.error('生成失败：' + m); },
        onDone: async () => { setStreaming(false); setStreamingPath(null); setStatusText(''); await reload(bookId, selection); toast.success('✓ 已重写'); },
      });
    }
  };

  // —— 章节推进（下一章 / 抽打）——
  const runChapter = (mode: 'next' | 'whip', whip?: string) => {
    const sectionId = selection.kind === 'chapter' ? selection.sectionId : tree.sections[0]?.id;
    if (!sectionId) { toast.error('请先新建一个部'); return; }
    const chapterId = selection.kind === 'chapter' ? selection.chapterId : undefined;
    setStreaming(true); setStreamingText(''); setStreamingPath('chapter');
    setStatusText(mode === 'whip' ? '🗯️ 正在按你的要求重写…' : '✍️ 正在写下一章…');
    abortRef.current = api.streamGen('/api/gen/chapter', { bookId, sectionId, chapterId, mode, whip }, {
      onDelta: (d) => setStreamingText((t) => t + d),
      onError: (m) => { setStreaming(false); setStreamingPath(null); setStatusText(''); toast.error('生成失败：' + m); },
      onDone: async (e) => {
        setStreaming(false); setStreamingPath(null); setStatusText('');
        await reload(bookId, { kind: 'chapter', sectionId, chapterId: e.chapterId ?? chapterId! });
        toast.success('✓ 本章完成');
      },
    });
  };

  const runSections = () => {
    setPlanOpen(true); setPlanText(''); setStreaming(true); setStatusText('🧩 正在规划分部…');
    abortRef.current = api.streamGen('/api/gen/sections', { bookId }, {
      onDelta: (d) => setPlanText((t) => t + d),
      onError: (m) => { setStreaming(false); setStatusText(''); toast.error('规划失败：' + m); },
      onDone: (e) => { setStreaming(false); setStatusText(''); if (e.sections) setPlanText(e.sections); },
    });
  };
  const adoptSections = async (titles: string[]) => {
    setPlanOpen(false);
    for (const t of titles) await api.addSection(bookId, t);
    await reload(bookId); toast.success(`✓ 已创建 ${titles.length} 个部`);
  };
  const stopGen = () => { abortRef.current?.(); setStreaming(false); setStreamingPath(null); setStatusText(''); };

  return (
    <div className="app">
      <TopBar title={tree.book.title} streaming={streaming} statusText={statusText}
        onOpenSettings={() => setShowSettings(true)} onHome={goShelf} />
      <div className="body">
        <Sidebar tree={tree} selection={selection} disabled={streaming}
          onSelect={setSelection}
          onAddSection={async () => { await api.addSection(bookId); await reload(bookId); }}
          onAddChapter={async (sid) => { const c = await api.addChapter(bookId, sid); await reload(bookId, { kind: 'chapter', sectionId: sid, chapterId: c.id }); }}
          onPlanSections={runSections} />
        <div className="content">
          <MainPanel tree={tree} selection={selection}
            streaming={streaming} streamingText={streamingText} streamingPath={streamingPath}
            onMove={doMove} onRewrite={doRewrite} onClear={doClear} onSave={doSave} onStop={stopGen} />
          {selection.kind === 'chapter' &&
            <Actions streaming={streaming} onNext={() => runChapter('next')} onWhip={(t) => runChapter('whip', t)} onStop={stopGen} />}
        </div>
      </div>
      {planOpen && <SectionPlanPanel text={planText} streaming={streaming}
        onAdopt={adoptSections} onClose={() => { if (streaming) stopGen(); setPlanOpen(false); }} />}
    </div>
  );
}
```

- [ ] **Step 4: 给 TopBar 加 `onHome`**（`web/src/components/TopBar.tsx`）

在 props 类型加 `onHome: () => void;`，并在 `.topbar-title` 左侧加返回按钮：

```tsx
// props 解构加 onHome
// 在 <header className="topbar"> 内、书名前插入：
<button className="hbtn mini" onClick={onHome}>📚 书架</button>
```
（确保新 prop 被使用，App 已传入。）

- [ ] **Step 5: 追加/微调样式 `web/src/styles.css`**

```css
/* 章节视图正文框与底部动作条留白 */
.content .main { padding-bottom: 8px; }
```

- [ ] **Step 6: 运行全部测试 + 构建**

Run（cwd=web）: `npm test`
Expected: PASS（versioned + 既有全绿）。
Run: `npm run build`
Expected: `tsc -b` 零错误、vite 构建成功（此刻全绿收口）。
Run: `grep -rn "alert(" src`
Expected: 无输出。

- [ ] **Step 7: 后端全量回归**

Run（仓库根）: `node --test server/test/*.test.js`
Expected: 全 PASS。

- [ ] **Step 8: 提交**

```bash
git add web/src/components/MainPanel.tsx web/src/components/Actions.tsx web/src/App.tsx web/src/components/TopBar.tsx web/src/styles.css
git commit -m "feat(web): 书架为启动落点 + 三视图版本化框 + 动作上下文化，收口全绿"
```

---

## Self-Review

**1. Spec coverage：**
- §2 数据模型 + 迁移 → Task 1（原语/parse/migrate）+ Task 2（惰性迁移/派生 content/createBook/addChapter）。✅
- §3.1 统一 move/clear/save → Task 4；§3.1 rewrite（SSE）→ Task 5（outline/core）+ 章节走 gen/chapter（记录的偏差）。✅
- §3.2 core 字段指令 → Task 3；buildSystemPrompt/buildContext 版本化兼容 → Task 3。✅
- §3.3 书架 listBooks 计数 / DELETE / PATCH → Task 2 + Task 4。✅
- §3.4 删旧端点 → Task 4（outline/core/saveChapter/rollback）+ Task 5（gen/outline）。✅
- §4 VersionedBox + 动作上下文化 → Task 7 + Task 9（Actions 收窄 + MainPanel 三视图 + Actions 仅章节渲染）。✅
- §5 书架 → Task 8 + Task 9（启动落点/返回/新建/改名/删除/色书脊）。✅
- §6 文件清单 → 分散在各任务，全覆盖。✅
- §7 测试策略 → 各任务的 node:test / vitest；§8 迁移风险 → Task 1/2 迁移充分单测 + 惰性迁移 + 原子写。✅

**2. Placeholder scan：** 无 TBD/TODO；每步含真实代码/命令。Task 5 gen.test 适配按"读该文件既有 stub 风格"描述——因该文件内容未在计划内全文引用，实现者需读文件后照风格补断言，这是必要的现场适配而非占位（已明确要断言什么）。✅

**3. Type/契约一致性：**
- `Versioned{versions,cursor}` 前后端同构。
- path 白名单在 `parseVersionPath`（后端）与 App 分派（`startsWith('section:')`/`'core:'`/`'outline'`）一致。
- streamingPath 取值集合（`'outline'|'core:*'|'chapter'|null`）在 App 设置与 MainPanel 判断一致。
- `versionMove/versionSet` 返回 `{versions,cursor}`，前端 move/clear/save 后统一 `reload`（不直接用返回值渲染，避免与 tree 不同步）。✅
- 章节 content 派生：仅在 `writeChapter` 内同步，所有写 body 路径（versionSet/gen chapter）都经 writeChapter。✅

**关于任务顺序与临时红灯：** Task 6 改 types/api 后，Task 7/8 新增组件可独立编译，但 MainPanel/App/Actions 直到 Task 9 才全绿——与上一轮同样是刻意的任务边界，Task 6/7/8 审查以"新文件自身类型正确 + 错误仅在未改造的老组件"为准，Task 9 达成并强制全绿。

**关于 spec 偏差（version/rewrite 不含章节）：** 已在 Task 5 接口块显式记录，理由是避免复制章节生成管线（上下文+digest+空章回滚），符合去重原则；前端按 path 分派，用户体验一致（章节框「重写」照样工作）。最终审查会看到此说明。

---

## Execution Handoff

Plan complete and saved to `docs/superpowers/plans/2026-07-30-书架与版本化框.md`.
